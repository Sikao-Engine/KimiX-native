// GENERATED by scripts/gen_bash_fix_data.py - DO NOT EDIT BY HAND.
//
// Full expected commands (fallback definitions + conditional exports +
// rewritten source) and the interactive compatibility prelude, taken
// verbatim from the reference implementation.
struct bash_fix_prefix_golden {
    const char *command;
    const char *expected_command;
};

const bash_fix_prefix_golden k_bash_fix_prefix_goldens[] = {
    {"gtimeout", "if ! command -v gtimeout >/dev/null 2>&1; then gtimeout() { local __kimix_native=''; __kimix_native=$(type -P gtimeout) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; timeout \"$@\"; }; fi\012if declare -F gtimeout >/dev/null; then export -f gtimeout; fi\012gtimeout"},
    {"rev", "if ! command -v rev >/dev/null 2>&1; then rev() { local __kimix_native=''; __kimix_native=$(type -P rev) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_zero=0; while (( $# )); do case $1 in -0|--zero) __kimix_zero=1; shift;; --) shift; break;; -*) printf '%s\\n' \"rev: unsupported option: $1\" >&2; return 1;; *) break;; esac; done; perl '-Mopen=:std,:encoding(UTF-8)' -e 'my $zero = shift @ARGV; my $failed = 0; sub reverse_fh { my ($fh, $zero) = @_; local $/ = $zero ? qq(\\0) : qq(\\n); while (my $record = <$fh>) { my $ended = $zero ? $record =~ s/\\0\\z// : $record =~ s/\\r?\\n\\z//; print scalar reverse($record); print($zero ? qq(\\0) : qq(\\n)) if $ended } } if (@ARGV) { for my $file (@ARGV) { if (open my $fh, q(<:encoding(UTF-8)), $file) { reverse_fh($fh, $zero); close $fh } else { warn qq(rev: $file: $!\\n); $failed = 1 } } } else { reverse_fh(*STDIN, $zero) } exit $failed' -- \"$__kimix_zero\" \"$@\"; }; fi\012if declare -F rev >/dev/null; then export -f rev; fi\012rev"},
    {"xdg-open", "if ! command -v xdg-open >/dev/null 2>&1; then xdg-open() { local __kimix_native=''; __kimix_native=$(type -P xdg-open) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; start \"$@\"; }; fi\012if declare -F xdg-open >/dev/null; then export -f xdg-open; fi\012xdg-open"},
    {"open", "if ! command -v open >/dev/null 2>&1; then open() { local __kimix_native=''; __kimix_native=$(type -P open) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; start \"$@\"; }; fi\012if declare -F open >/dev/null; then export -f open; fi\012open"},
    {"pbcopy", "if ! command -v pbcopy >/dev/null 2>&1; then pbcopy() { local __kimix_native=''; __kimix_native=$(type -P pbcopy) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; clip.exe \"$@\"; }; fi\012if declare -F pbcopy >/dev/null; then export -f pbcopy; fi\012pbcopy"},
    {"pbpaste", "if ! command -v pbpaste >/dev/null 2>&1; then pbpaste() { local __kimix_native=''; __kimix_native=$(type -P pbpaste) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; powershell.exe -NoProfile -NonInteractive -Command '[Console]::OutputEncoding=[System.Text.Encoding]::UTF8;[Console]::Out.Write((Get-Clipboard -Raw))' \"$@\"; }; fi\012if declare -F pbpaste >/dev/null; then export -f pbpaste; fi\012pbpaste"},
    {"wget", "if ! command -v wget >/dev/null 2>&1; then wget() { local __kimix_native=''; __kimix_native=$(type -P wget) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_url='' __kimix_out='' __kimix_stdout=0; local -a __kimix_args=(); while (( $# )); do case $1 in -O|--output-document) __kimix_out=$2; shift 2;; -O?*) __kimix_out=${1#-O}; shift;; --output-document=*) __kimix_out=${1#*=}; shift;; -q|--quiet) __kimix_args+=(-s); shift;; -c|--continue) __kimix_args+=(-C -); shift;; --no-check-certificate) __kimix_args+=(-k); shift;; -T|--timeout) __kimix_args+=(--max-time \"$2\"); shift 2;; --timeout=*) __kimix_args+=(--max-time \"${1#*=}\"); shift;; -*) printf '%s\\n' \"wget: unsupported option for curl fallback: $1\" >&2; return 1;; *) __kimix_url=$1; shift;; esac; done; if [[ -z $__kimix_url ]]; then printf '%s\\n' 'wget: missing URL' >&2; return 1; fi; if [[ $__kimix_out == '-' ]]; then __kimix_stdout=1; fi; if [[ -z $__kimix_out && $__kimix_stdout -eq 0 ]]; then __kimix_out=${__kimix_url##*/}; [[ -n $__kimix_out ]] || __kimix_out=index.html; fi; if (( __kimix_stdout )); then curl -fSL \"${__kimix_args[@]}\" -- \"$__kimix_url\"; else curl -fSL \"${__kimix_args[@]}\" -o \"$__kimix_out\" -- \"$__kimix_url\"; fi; }; fi\012if declare -F wget >/dev/null; then export -f wget; fi\012wget"},
    {"xclip", "if ! command -v xclip >/dev/null 2>&1; then xclip() { local __kimix_native=''; __kimix_native=$(type -P xclip) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_out=0; while (( $# )); do case $1 in -o|-out) __kimix_out=1; shift;; -i|-in) shift;; -selection|-d|-display) shift 2;; -selection*|-display*) shift;; -*) printf '%s\\n' \"xclip: unsupported option for clipboard fallback: $1\" >&2; return 1;; *) shift;; esac; done; if (( __kimix_out )); then powershell.exe -NoProfile -NonInteractive -Command '[Console]::OutputEncoding=[System.Text.Encoding]::UTF8;[Console]::Out.Write((Get-Clipboard -Raw))'; else clip.exe; fi; }; fi\012if declare -F xclip >/dev/null; then export -f xclip; fi\012xclip"},
    {"xsel", "if ! command -v xsel >/dev/null 2>&1; then xsel() { local __kimix_native=''; __kimix_native=$(type -P xsel) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_out=0; while (( $# )); do case $1 in --output) __kimix_out=1; shift;; --input|--clipboard|--primary|--secondary) shift;; --*) printf '%s\\n' \"xsel: unsupported option for clipboard fallback: $1\" >&2; return 1;; -*) case $1 in *o*) __kimix_out=1;; esac; shift;; *) shift;; esac; done; if (( __kimix_out )); then powershell.exe -NoProfile -NonInteractive -Command '[Console]::OutputEncoding=[System.Text.Encoding]::UTF8;[Console]::Out.Write((Get-Clipboard -Raw))'; else clip.exe; fi; }; fi\012if declare -F xsel >/dev/null; then export -f xsel; fi\012xsel"},
    {"wl-copy", "if ! command -v wl-copy >/dev/null 2>&1; then wl-copy() { local __kimix_native=''; __kimix_native=$(type -P wl-copy) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; while (( $# )); do case $1 in -*) printf '%s\\n' \"wl-copy: unsupported option for clipboard fallback: $1\" >&2; return 1;; *) shift;; esac; done; clip.exe; }; fi\012if declare -F wl-copy >/dev/null; then export -f wl-copy; fi\012wl-copy"},
    {"wl-paste", "if ! command -v wl-paste >/dev/null 2>&1; then wl-paste() { local __kimix_native=''; __kimix_native=$(type -P wl-paste) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; while (( $# )); do case $1 in -n|--no-newline) shift;; -*) printf '%s\\n' \"wl-paste: unsupported option for clipboard fallback: $1\" >&2; return 1;; *) shift;; esac; done; powershell.exe -NoProfile -NonInteractive -Command '[Console]::OutputEncoding=[System.Text.Encoding]::UTF8;[Console]::Out.Write((Get-Clipboard -Raw))'; }; fi\012if declare -F wl-paste >/dev/null; then export -f wl-paste; fi\012wl-paste"},
    {"zip", "if ! command -v zip >/dev/null 2>&1; then zip() { local __kimix_native=''; __kimix_native=$(type -P zip) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_archive='' __kimix_level=Optimal __kimix_p='' __kimix_combo='' __kimix_i=0; local -a __kimix_paths=() __kimix_wpaths=() __kimix_split=(); while (( $# )); do if [[ $1 == -[!-]* && ${#1} -gt 2 ]]; then __kimix_combo=${1#-}; __kimix_split=(); shift; for (( __kimix_i=0; __kimix_i<${#__kimix_combo}; __kimix_i++ )); do __kimix_split+=(-${__kimix_combo:__kimix_i:1}); done; set -- \"${__kimix_split[@]}\" \"$@\"; continue; fi; case $1 in -r|-R|--recurse-paths|-q|--quiet) shift;; -0) __kimix_level=NoCompression; shift;; -1) __kimix_level=Fastest; shift;; -[2-9]) shift;; -*) printf '%s\\n' \"zip: unsupported option for Compress-Archive fallback: $1\" >&2; return 1;; *) if [[ -z $__kimix_archive ]]; then __kimix_archive=$1; else __kimix_paths+=(\"$1\"); fi; shift;; esac; done; if [[ -z $__kimix_archive || ${#__kimix_paths[@]} -eq 0 ]]; then printf '%s\\n' 'zip: missing archive name or input paths' >&2; return 1; fi; for __kimix_p in \"${__kimix_paths[@]}\"; do __kimix_wpaths+=(\"$(cygpath -w -- \"$__kimix_p\")\"); done; __kimix_archive=$(cygpath -w -- \"$__kimix_archive\"); __KIMIX_ZIP_LEVEL=$__kimix_level __KIMIX_ZIP_DEST=$__kimix_archive __KIMIX_ZIP_PATHS=$(printf '%s\\n' \"${__kimix_wpaths[@]}\") powershell.exe -NoProfile -NonInteractive -Command 'Add-Type -AssemblyName System.IO.Compression, System.IO.Compression.FileSystem; $level = [System.IO.Compression.CompressionLevel]$env:__KIMIX_ZIP_LEVEL; $dest = $env:__KIMIX_ZIP_DEST; if (Test-Path -LiteralPath $dest) { Remove-Item -LiteralPath $dest -Force }; $zip = [System.IO.Compression.ZipFile]::Open($dest, [System.IO.Compression.ZipArchiveMode]::Create); foreach ($p in ($env:__KIMIX_ZIP_PATHS -split \"`n\")) { $item = Get-Item -LiteralPath $p; $base = $item.Name; if ($item.PSIsContainer) { $root = $item.FullName; Get-ChildItem -LiteralPath $root -Recurse -Force | ForEach-Object { $rel = $_.FullName.Substring($root.Length).TrimStart(\"\\\") -replace \"\\\\\", \"/\"; if ($_.PSIsContainer) { $zip.CreateEntry($base + \"/\" + $rel + \"/\") | Out-Null } else { [System.IO.Compression.ZipFileExtensions]::CreateEntryFromFile($zip, $_.FullName, $base + \"/\" + $rel, $level) | Out-Null } } } else { [System.IO.Compression.ZipFileExtensions]::CreateEntryFromFile($zip, $item.FullName, $base, $level) | Out-Null } }; $zip.Dispose(); if (Test-Path -LiteralPath $dest) { exit 0 } else { exit 1 }'; }; fi\012if declare -F zip >/dev/null; then export -f zip; fi\012zip"},
    {"nc", "if ! command -v nc >/dev/null 2>&1; then nc() { local __kimix_native=''; __kimix_native=$(type -P nc) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_z=0 __kimix_v=0 __kimix_w='' __kimix_host='' __kimix_port=''; while (( $# )); do case $1 in -z) __kimix_z=1; shift;; -v) __kimix_v=1; shift;; -zv|-vz) __kimix_z=1; __kimix_v=1; shift;; -w) __kimix_w=$2; shift 2;; -w?*) __kimix_w=${1#-w}; shift;; -*) printf '%s\\n' \"nc: unsupported option for /dev/tcp fallback: $1\" >&2; return 1;; *) if [[ -z $__kimix_host ]]; then __kimix_host=$1; elif [[ -z $__kimix_port ]]; then __kimix_port=$1; else printf '%s\\n' 'nc: too many arguments' >&2; return 1; fi; shift;; esac; done; if (( ! __kimix_z )); then printf '%s\\n' 'nc: only -z (zero-I/O scan) mode is supported by this fallback' >&2; return 1; fi; if [[ -z $__kimix_host || -z $__kimix_port ]]; then printf '%s\\n' 'nc: missing host or port' >&2; return 1; fi; if [[ -n $__kimix_w ]]; then timeout \"$__kimix_w\" bash -c 'exec 3<>/dev/tcp/$1/$2' _ \"$__kimix_host\" \"$__kimix_port\" 2>/dev/null; else (exec 3<>/dev/tcp/\"$__kimix_host\"/\"$__kimix_port\") 2>/dev/null; fi; local __kimix_rc=$?; (( __kimix_rc != 0 )) && __kimix_rc=1; if (( __kimix_rc == 0 )); then (( __kimix_v )) && printf '%s\\n' \"Connection to $__kimix_host $__kimix_port port [tcp/*] succeeded!\" >&2; else (( __kimix_v )) && printf '%s\\n' \"nc: connect to $__kimix_host port $__kimix_port (tcp) failed\" >&2; fi; return $__kimix_rc; }; fi\012if declare -F nc >/dev/null; then export -f nc; fi\012nc"},
    {"pgrep", "if ! command -v pgrep >/dev/null 2>&1; then pgrep() { local __kimix_native=''; __kimix_native=$(type -P pgrep) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_list=0 __kimix_full=0 __kimix_pat=''; while (( $# )); do case $1 in -l) __kimix_list=1; shift;; -f) __kimix_full=1; shift;; -lf|-fl) __kimix_list=1; __kimix_full=1; shift;; --) shift; break;; -*) printf '%s\\n' \"pgrep: unsupported option for Get-Process fallback: $1\" >&2; return 1;; *) __kimix_pat=$1; shift;; esac; done; if [[ -z $__kimix_pat ]]; then printf '%s\\n' 'pgrep: missing pattern' >&2; return 1; fi; if (( __kimix_full )); then __KIMIX_PAT=$__kimix_pat __KIMIX_LIST=$__kimix_list powershell.exe -NoProfile -NonInteractive -Command '$m = Get-CimInstance Win32_Process | Where-Object { $_.CommandLine -match $env:__KIMIX_PAT }; if ($m) { $m | ForEach-Object { if ($env:__KIMIX_LIST -eq \"1\") { \"$($_.ProcessId) $($_.Name)\" } else { $_.ProcessId } }; exit 0 } else { exit 1 }'; else __KIMIX_PAT=$__kimix_pat __KIMIX_LIST=$__kimix_list powershell.exe -NoProfile -NonInteractive -Command '$m = Get-Process | Where-Object { $_.Name -match $env:__KIMIX_PAT }; if ($m) { $m | ForEach-Object { if ($env:__KIMIX_LIST -eq \"1\") { \"$($_.Id) $($_.Name)\" } else { $_.Id } }; exit 0 } else { exit 1 }'; fi; }; fi\012if declare -F pgrep >/dev/null; then export -f pgrep; fi\012pgrep"},
    {"pkill", "if ! command -v pkill >/dev/null 2>&1; then pkill() { local __kimix_native=''; __kimix_native=$(type -P pkill) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_full=0 __kimix_pat=''; while (( $# )); do case $1 in -f) __kimix_full=1; shift;; --) shift; break;; -*) printf '%s\\n' \"pkill: unsupported option for Stop-Process fallback: $1\" >&2; return 1;; *) __kimix_pat=$1; shift;; esac; done; if [[ -z $__kimix_pat ]]; then printf '%s\\n' 'pkill: missing pattern' >&2; return 1; fi; if (( __kimix_full )); then __KIMIX_PAT=$__kimix_pat powershell.exe -NoProfile -NonInteractive -Command '$m = Get-CimInstance Win32_Process | Where-Object { $_.CommandLine -match $env:__KIMIX_PAT }; if ($m) { $m | ForEach-Object { Stop-Process -Id $_.ProcessId -Force }; exit 0 } else { exit 1 }'; else __KIMIX_PAT=$__kimix_pat powershell.exe -NoProfile -NonInteractive -Command '$m = Get-Process | Where-Object { $_.Name -match $env:__KIMIX_PAT }; if ($m) { $m | Stop-Process -Force; exit 0 } else { exit 1 }'; fi; }; fi\012if declare -F pkill >/dev/null; then export -f pkill; fi\012pkill"},
    {"traceroute", "if ! command -v traceroute >/dev/null 2>&1; then traceroute() { local __kimix_native=''; __kimix_native=$(type -P traceroute) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local -a __kimix_args=(); while (( $# )); do case $1 in -n) __kimix_args+=(-d); shift;; -m) __kimix_args+=(-h \"$2\"); shift 2;; -m?*) __kimix_args+=(-h \"${1#-m}\"); shift;; --max-hop=*) __kimix_args+=(-h \"${1#*=}\"); shift;; -w) __kimix_args+=(-w \"$(( $2 * 1000 ))\"); shift 2;; -w?*) __kimix_args+=(-w \"$(( ${1#-w} * 1000 ))\"); shift;; -*) printf '%s\\n' \"traceroute: unsupported option for tracert fallback: $1\" >&2; return 1;; *) __kimix_args+=(\"$1\"); shift;; esac; done; tracert \"${__kimix_args[@]}\"; }; fi\012if declare -F traceroute >/dev/null; then export -f traceroute; fi\012traceroute"},
    {"tree", "if ! command -v tree >/dev/null 2>&1; then tree() { local __kimix_native=''; __kimix_native=$(type -P tree) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_depth=0 __kimix_all=0 __kimix_dirs=0 __kimix_noreport=0 __kimix_dir=''; while (( $# )); do case $1 in -L) __kimix_depth=$2; shift 2;; -L?*) __kimix_depth=${1#-L}; shift;; -a) __kimix_all=1; shift;; -d) __kimix_dirs=1; shift;; --noreport) __kimix_noreport=1; shift;; --) shift; break;; -*) printf '%s\\n' \"tree: unsupported option for perl fallback: $1\" >&2; return 1;; *) __kimix_dir=$1; shift;; esac; done; [[ -n $__kimix_dir ]] || __kimix_dir=.; perl -e 'my ($maxdepth,$showall,$dirsonly,$noreport,$top)=@ARGV; print qq($top\\n); my ($ndirs,$nfiles)=(0,0); sub walk { my ($path,$prefix,$depth)=@_; return if $maxdepth && $depth>$maxdepth; opendir(my $dh,$path) or return; my @e = grep { ! /^[.][.]?$/ } readdir($dh); closedir($dh); @e = grep { $showall || ! /^[.]/ } @e; @e = grep { ! $dirsonly || -d qq($path/$_) } @e; @e = sort { lc($a) cmp lc($b) } @e; my $n=@e; my $i=0; for my $e (@e) { $i++; my $last = $i==$n; my $full = qq($path/$e); my $isdir = -d $full; if ($isdir) { $ndirs++ } else { $nfiles++ } print $prefix, ($last ? qq(`-- ) : qq(|-- )), $e, qq(\\n); walk($full, $prefix . ($last ? qq(    ) : qq(|   )), $depth+1) if $isdir && ! -l $full } } walk($top,q(),1); my $dw = $ndirs==1 ? q(directory) : q(directories); my $fw = $nfiles==1 ? q(file) : q(files); print qq(\\n$ndirs $dw, $nfiles $fw\\n) unless $noreport' -- \"$__kimix_depth\" \"$__kimix_all\" \"$__kimix_dirs\" \"$__kimix_noreport\" \"$__kimix_dir\"; }; fi\012if declare -F tree >/dev/null; then export -f tree; fi\012tree"},
    {"say", "if ! command -v say >/dev/null 2>&1; then say() { local __kimix_native=''; __kimix_native=$(type -P say) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; while (( $# )); do case $1 in -*) printf '%s\\n' \"say: unsupported option for SAPI fallback: $1\" >&2; return 1;; *) shift;; esac; done; __KIMIX_SAY_TEXT=$* powershell.exe -NoProfile -NonInteractive -Command 'Add-Type -AssemblyName System.Speech; (New-Object System.Speech.Synthesis.SpeechSynthesizer).Speak($env:__KIMIX_SAY_TEXT)'; }; fi\012if declare -F say >/dev/null; then export -f say; fi\012say"},
    {"python3", "if ! command -v python3 >/dev/null 2>&1 || [[ $(type -P python3) == *WindowsApps* ]]; then python3() { local __kimix_native=''; __kimix_native=$(type -P python3) || :; if [[ -n $__kimix_native && $__kimix_native != *WindowsApps* ]]; then \"$__kimix_native\" \"$@\"; return; fi; python \"$@\"; }; fi\012if declare -F python3 >/dev/null; then export -f python3; fi\012python3"},
    {"pip3", "if ! command -v pip3 >/dev/null 2>&1 || [[ $(type -P pip3) == *WindowsApps* ]]; then pip3() { local __kimix_native=''; __kimix_native=$(type -P pip3) || :; if [[ -n $__kimix_native && $__kimix_native != *WindowsApps* ]]; then \"$__kimix_native\" \"$@\"; return; fi; pip \"$@\"; }; fi\012if declare -F pip3 >/dev/null; then export -f pip3; fi\012pip3"},
    {"copy", "if ! command -v copy >/dev/null 2>&1; then copy() { local __kimix_native=''; __kimix_native=$(type -P copy) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; if [[ $# -lt 2 ]]; then printf '%s\\n' 'copy: missing source or destination' >&2; return 1; fi; cp -R -- \"$@\"; }; fi\012if declare -F copy >/dev/null; then export -f copy; fi\012copy"},
    {"move", "if ! command -v move >/dev/null 2>&1; then move() { local __kimix_native=''; __kimix_native=$(type -P move) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; if [[ $# -lt 2 ]]; then printf '%s\\n' 'move: missing source or destination' >&2; return 1; fi; mv -- \"$@\"; }; fi\012if declare -F move >/dev/null; then export -f move; fi\012move"},
    {"del", "if ! command -v del >/dev/null 2>&1; then del() { local __kimix_native=''; __kimix_native=$(type -P del) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; rm -- \"$@\"; }; fi\012if declare -F del >/dev/null; then export -f del; fi\012del"},
    {"erase", "if ! command -v erase >/dev/null 2>&1; then erase() { local __kimix_native=''; __kimix_native=$(type -P erase) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; rm -- \"$@\"; }; fi\012if declare -F erase >/dev/null; then export -f erase; fi\012erase"},
    {"ren", "if ! command -v ren >/dev/null 2>&1; then ren() { local __kimix_native=''; __kimix_native=$(type -P ren) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; if [[ $# -ne 2 ]]; then printf '%s\\n' 'ren: exactly two arguments required' >&2; return 1; fi; mv -- \"$1\" \"$2\"; }; fi\012if declare -F ren >/dev/null; then export -f ren; fi\012ren"},
    {"rename", "if ! command -v rename >/dev/null 2>&1; then rename() { local __kimix_native=''; __kimix_native=$(type -P rename) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; if [[ $# -ne 2 ]]; then printf '%s\\n' 'rename: exactly two arguments required' >&2; return 1; fi; mv -- \"$1\" \"$2\"; }; fi\012if declare -F rename >/dev/null; then export -f rename; fi\012rename"},
    {"rd", "if ! command -v rd >/dev/null 2>&1; then rd() { local __kimix_native=''; __kimix_native=$(type -P rd) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; rmdir -- \"$@\"; }; fi\012if declare -F rd >/dev/null; then export -f rd; fi\012rd"},
    {"md", "if ! command -v md >/dev/null 2>&1; then md() { local __kimix_native=''; __kimix_native=$(type -P md) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; mkdir -p -- \"$@\"; }; fi\012if declare -F md >/dev/null; then export -f md; fi\012md"},
    {"chdir", "if ! command -v chdir >/dev/null 2>&1; then chdir() { local __kimix_native=''; __kimix_native=$(type -P chdir) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; cd -- \"$@\"; }; fi\012if declare -F chdir >/dev/null; then export -f chdir; fi\012chdir"},
    {"cls", "if ! command -v cls >/dev/null 2>&1; then cls() { local __kimix_native=''; __kimix_native=$(type -P cls) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; clear; }; fi\012if declare -F cls >/dev/null; then export -f cls; fi\012cls"},
    {"xcopy", "if ! command -v xcopy >/dev/null 2>&1; then xcopy() { local __kimix_native=''; __kimix_native=$(type -P xcopy) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; cp -r -- \"$@\"; }; fi\012if declare -F xcopy >/dev/null; then export -f xcopy; fi\012xcopy"},
    {"mklink", "if ! command -v mklink >/dev/null 2>&1; then mklink() { local __kimix_native=''; __kimix_native=$(type -P mklink) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_hard=0 __kimix_link='' __kimix_target=''; while (( $# )); do case $1 in /D|/d|/J|/j) shift;; /H|/h) __kimix_hard=1; shift;; *) if [[ -z $__kimix_link ]]; then __kimix_link=$1; elif [[ -z $__kimix_target ]]; then __kimix_target=$1; else printf '%s\\n' 'mklink: too many arguments' >&2; return 1; fi; shift;; esac; done; if [[ -z $__kimix_link || -z $__kimix_target ]]; then printf '%s\\n' 'mklink: missing link name or target' >&2; return 1; fi; if (( __kimix_hard )); then ln -f -- \"$__kimix_target\" \"$__kimix_link\"; else ln -s -- \"$__kimix_target\" \"$__kimix_link\"; fi; }; fi\012if declare -F mklink >/dev/null; then export -f mklink; fi\012mklink"},
    {"findstr", "if ! command -v findstr >/dev/null 2>&1; then findstr() { local __kimix_native=''; __kimix_native=$(type -P findstr) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; grep \"$@\"; }; fi\012if declare -F findstr >/dev/null; then export -f findstr; fi\012findstr"},
    {"fc", "if ! command -v fc >/dev/null 2>&1; then fc() { local __kimix_native=''; __kimix_native=$(type -P fc) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; diff \"$@\"; }; fi\012if declare -F fc >/dev/null; then export -f fc; fi\012fc"},
    {"where", "if ! command -v where >/dev/null 2>&1; then where() { local __kimix_native=''; __kimix_native=$(type -P where) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; which \"$@\"; }; fi\012if declare -F where >/dev/null; then export -f where; fi\012where"},
    {"tasklist", "if ! command -v tasklist >/dev/null 2>&1; then tasklist() { local __kimix_native=''; __kimix_native=$(type -P tasklist) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; powershell.exe -NoProfile -NonInteractive -Command 'Get-Process | Select-Object Name, Id, CPU, WorkingSet | Format-Table -AutoSize'; }; fi\012if declare -F tasklist >/dev/null; then export -f tasklist; fi\012tasklist"},
    {"taskkill", "if ! command -v taskkill >/dev/null 2>&1; then taskkill() { local __kimix_native=''; __kimix_native=$(type -P taskkill) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_force=0 __kimix_pid='' __kimix_im=''; while (( $# )); do case $1 in /F|/f) __kimix_force=1; shift;; /IM|/im) __kimix_im=$2; shift 2;; /PID|/pid) __kimix_pid=$2; shift 2;; /*) printf '%s\\n' \"taskkill: unsupported option: $1\" >&2; return 1;; *) printf '%s\\n' \"taskkill: unsupported argument: $1\" >&2; return 1;; esac; done; if [[ -n $__kimix_pid ]]; then __KIMIX_FORCE=$__kimix_force __KIMIX_PID=$__kimix_pid powershell.exe -NoProfile -NonInteractive -Command '$force = $env:__KIMIX_FORCE -eq '1'; if ($env:__KIMIX_PID) { Stop-Process -Id $env:__KIMIX_PID -Force:$force; exit 0 } $procs = Get-Process | Where-Object { $_.Name -eq $env:__KIMIX_IM }; if ($procs) { $procs | Stop-Process -Force:$force; exit 0 } else { exit 1 }'; elif [[ -n $__kimix_im ]]; then __KIMIX_FORCE=$__kimix_force __KIMIX_IM=$__kimix_im powershell.exe -NoProfile -NonInteractive -Command '$force = $env:__KIMIX_FORCE -eq '1'; if ($env:__KIMIX_PID) { Stop-Process -Id $env:__KIMIX_PID -Force:$force; exit 0 } $procs = Get-Process | Where-Object { $_.Name -eq $env:__KIMIX_IM }; if ($procs) { $procs | Stop-Process -Force:$force; exit 0 } else { exit 1 }'; else printf '%s\\n' 'taskkill: missing /PID or /IM' >&2; return 1; fi; }; fi\012if declare -F taskkill >/dev/null; then export -f taskkill; fi\012taskkill"},
    {"systeminfo", "if ! command -v systeminfo >/dev/null 2>&1; then systeminfo() { local __kimix_native=''; __kimix_native=$(type -P systeminfo) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; powershell.exe -NoProfile -NonInteractive -Command 'Get-ComputerInfo | Format-List'; }; fi\012if declare -F systeminfo >/dev/null; then export -f systeminfo; fi\012systeminfo"},
    {"watch", "if ! command -v watch >/dev/null 2>&1; then watch() { local __kimix_native=''; __kimix_native=$(type -P watch) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_interval=2; while (( $# )); do case $1 in -n) __kimix_interval=$2; shift 2;; -n?*) __kimix_interval=${1#-n}; shift;; -t|-d|--no-title|--color) shift;; --) shift; break;; -*) printf '%s\\n' \"watch: unsupported option: $1\" >&2; return 1;; *) break;; esac; done; if [[ $# -eq 0 ]]; then printf '%s\\n' 'watch: missing command' >&2; return 1; fi; while true; do clear; eval \"$*\"; sleep \"$__kimix_interval\"; done; }; fi\012if declare -F watch >/dev/null; then export -f watch; fi\012watch"},
    {"killall", "if ! command -v killall >/dev/null 2>&1; then killall() { local __kimix_native=''; __kimix_native=$(type -P killall) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; if [[ $# -eq 0 ]]; then printf '%s\\n' 'killall: missing process name' >&2; return 1; fi; __KIMIX_NAME=$1 powershell.exe -NoProfile -NonInteractive -Command '$procs = Get-Process | Where-Object { $_.Name -eq $env:__KIMIX_NAME }; if ($procs) { $procs | Stop-Process -Force; exit 0 } else { exit 1 }'; }; fi\012if declare -F killall >/dev/null; then export -f killall; fi\012killall"},
    {"pidof", "if ! command -v pidof >/dev/null 2>&1; then pidof() { local __kimix_native=''; __kimix_native=$(type -P pidof) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; if [[ $# -eq 0 ]]; then printf '%s\\n' 'pidof: missing process name' >&2; return 1; fi; __KIMIX_NAME=$1 powershell.exe -NoProfile -NonInteractive -Command '$ids = (Get-Process | Where-Object { $_.Name -eq $env:__KIMIX_NAME }).Id; if ($ids) { $ids -join \" \"; exit 0 } else { exit 1 }'; }; fi\012if declare -F pidof >/dev/null; then export -f pidof; fi\012pidof"},
    {"column", "if ! command -v column >/dev/null 2>&1; then column() { local __kimix_native=''; __kimix_native=$(type -P column) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_sep='DEFAULT'; while (( $# )); do case $1 in -t) shift;; -s) __kimix_sep=$2; shift 2;; -s?*) __kimix_sep=${1#-s}; shift;; -*) printf '%s\012' \"column: unsupported option for perl fallback: $1\" >&2; return 1;; *) break;; esac; done; perl -e 'my $sep = shift @ARGV; $sep = qr/\\s+/ if $sep eq \"DEFAULT\"; my @rows; my @max; while (<>) { chomp; my @c = split $sep; push @rows, \\@c; for my $i (0..$#c) { $max[$i] = length($c[$i]) if !defined $max[$i] || length($c[$i]) > $max[$i]; } } for my $r (@rows) { print join(\"  \", map { sprintf(\"%-*s\", $max[$_]//0, $r->[$_]) } 0..$#$r), \"\\n\"; }' \"$__kimix_sep\" \"$@\"; }; fi\012if declare -F column >/dev/null; then export -f column; fi\012column"},
    {"free", "if ! command -v free >/dev/null 2>&1; then free() { local __kimix_native=''; __kimix_native=$(type -P free) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_unit=K; while (( $# )); do case $1 in -b|--bytes) __kimix_unit=B; shift;; -k|--kibi|--kilo) __kimix_unit=K; shift;; -m|--mebi|--mega) __kimix_unit=M; shift;; -g|--gibi|--giga) __kimix_unit=G; shift;; -h|--human) __kimix_unit=H; shift;; --help) printf '%s\012' 'free: report memory usage (Windows: Win32_OperatingSystem; no swap row)'; return 0;; -*) printf '%s\012' \"free: unsupported option: $1\" >&2; return 1;; *) shift;; esac; done; local -a __kimix_m=(); mapfile -t __kimix_m < <(powershell.exe -NoProfile -NonInteractive -Command '$o=Get-CimInstance Win32_OperatingSystem; Write-Output $o.TotalVisibleMemorySize; Write-Output $o.FreePhysicalMemory' 2>/dev/null | tr -d '\\r'); if [[ ${#__kimix_m[@]} -lt 2 ]]; then printf '%s\012' 'free: failed to query memory information' >&2; return 1; fi; local __kimix_total=${__kimix_m[0]} __kimix_free=${__kimix_m[1]}; local __kimix_used=$(( __kimix_total - __kimix_free )); printf '%s\012' '               total        used        free'; case $__kimix_unit in B) printf 'Mem: %12d %11d %11d\012' $(( __kimix_total * 1024 )) $(( __kimix_used * 1024 )) $(( __kimix_free * 1024 ));; M) printf 'Mem: %12d %11d %11d\012' $(( __kimix_total / 1024 )) $(( __kimix_used / 1024 )) $(( __kimix_free / 1024 ));; G) printf 'Mem: %12d %11d %11d\012' $(( __kimix_total / 1024 / 1024 )) $(( __kimix_used / 1024 / 1024 )) $(( __kimix_free / 1024 / 1024 ));; H) awk -v t=\"$__kimix_total\" -v f=\"$__kimix_free\" 'function h(x,  i){i=1; while(x>=10240&&i<4){x=x/1024;i++} return sprintf(\"%.1f%s\", x, substr(\"KMGT\", i, 1))} BEGIN{printf \"Mem: %11s %10s %10s\\n\", h(t), h(t-f), h(f)}';; *) printf 'Mem: %12d %11d %11d\012' \"$__kimix_total\" \"$__kimix_used\" \"$__kimix_free\";; esac; }; fi\012if declare -F free >/dev/null; then export -f free; fi\012free"},
    {"uptime", "if ! command -v uptime >/dev/null 2>&1; then uptime() { local __kimix_native=''; __kimix_native=$(type -P uptime) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_since=0; while (( $# )); do case $1 in -s|--since) __kimix_since=1; shift;; --help) printf '%s\012' 'uptime: tell how long the system has been running (Windows approximation; no user count)'; return 0;; -*) printf '%s\012' \"uptime: unsupported option: $1\" >&2; return 1;; *) printf '%s\012' \"uptime: unsupported argument: $1\" >&2; return 1;; esac; done; local -a __kimix_u=(); mapfile -t __kimix_u < <(powershell.exe -NoProfile -NonInteractive -Command '$b=(Get-CimInstance Win32_OperatingSystem).LastBootUpTime; Write-Output $b.ToString(\"yyyy-MM-dd HH:mm:ss\"); $up=New-TimeSpan -Start $b -End (Get-Date); Write-Output $up.Days; Write-Output $up.Hours; Write-Output $up.Minutes' 2>/dev/null | tr -d '\\r'); if [[ ${#__kimix_u[@]} -lt 4 ]]; then printf '%s\012' 'uptime: failed to query boot time' >&2; return 1; fi; if (( __kimix_since )); then printf '%s\012' \"${__kimix_u[0]}\"; return 0; fi; local __kimix_days=${__kimix_u[1]} __kimix_hours=${__kimix_u[2]} __kimix_mins=${__kimix_u[3]}; printf -v __kimix_mins '%02d' \"$__kimix_mins\"; local __kimix_up; if (( __kimix_days > 0 )); then printf -v __kimix_up '%d day(s), %d:%s' \"$__kimix_days\" \"$__kimix_hours\" \"$__kimix_mins\"; else printf -v __kimix_up '%d:%s' \"$__kimix_hours\" \"$__kimix_mins\"; fi; printf '%s up %s, load average: n/a (not reported on Windows)\012' \"$(date '+%H:%M:%S')\" \"$__kimix_up\"; }; fi\012if declare -F uptime >/dev/null; then export -f uptime; fi\012uptime"},
    {"top", "if ! command -v top >/dev/null 2>&1; then top() { local __kimix_native=''; __kimix_native=$(type -P top) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_delay=3 __kimix_iters=0 __kimix_batch=0 __kimix_count=0; while (( $# )); do case $1 in -b) __kimix_batch=1; shift;; -d) __kimix_delay=$2; shift 2;; -d?*) __kimix_delay=${1#-d}; shift;; -n) __kimix_iters=$2; shift 2;; -n?*) __kimix_iters=${1#-n}; shift;; -h|--help) printf '%s\012' 'top: display processes (Windows approximation via Get-Process; Ctrl+C quits)'; return 0;; -*) printf '%s\012' \"${FUNCNAME[0]}: unsupported option: $1\" >&2; return 1;; *) shift;; esac; done; __kimix_snapshot() { powershell.exe -NoProfile -NonInteractive -Command 'Get-Process | Sort-Object -Property CPU -Descending | Select-Object -First 25 Id,ProcessName,CPU,WorkingSet | Format-Table -AutoSize'; }; if (( __kimix_batch )); then local __kimix_n=$__kimix_iters; (( __kimix_n > 0 )) || __kimix_n=1; while (( __kimix_count < __kimix_n )); do __kimix_snapshot; __kimix_count=$(( __kimix_count + 1 )); done; return 0; fi; while (( __kimix_iters == 0 || __kimix_count < __kimix_iters )); do clear; __kimix_snapshot; __kimix_count=$(( __kimix_count + 1 )); (( __kimix_iters > 0 && __kimix_count >= __kimix_iters )) && break; sleep \"$__kimix_delay\"; done; }; fi\012if declare -F top >/dev/null; then export -f top; fi\012top"},
    {"ss", "if ! command -v ss >/dev/null 2>&1; then ss() { local __kimix_native=''; __kimix_native=$(type -P ss) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_stats=0; local -a __kimix_split=(); local __kimix_combo='' __kimix_i=0; while (( $# )); do if [[ $1 == -[!-]* && ${#1} -gt 2 ]]; then __kimix_combo=${1#-}; __kimix_split=(); shift; for (( __kimix_i=0; __kimix_i<${#__kimix_combo}; __kimix_i++ )); do __kimix_split+=(-${__kimix_combo:__kimix_i:1}); done; set -- \"${__kimix_split[@]}\" \"$@\"; continue; fi; case $1 in -s|--summary) __kimix_stats=1; shift;; -t|-u|-l|-n|-a|-p|-e|-m|-r|-i|-x|-4|-6|-T|--tcp|--udp|--listening|--numeric|--all|--process|--extended|--memory|--resolve|--inet|--inet4|--inet6) shift;; --*) shift;; -*) printf '%s\012' \"ss: unsupported option: $1\" >&2; return 1;; *) shift;; esac; done; if (( __kimix_stats )); then netstat -s; else netstat -ano; fi; }; fi\012if declare -F ss >/dev/null; then export -f ss; fi\012ss"},
    {"ip", "if ! command -v ip >/dev/null 2>&1; then ip() { local __kimix_native=''; __kimix_native=$(type -P ip) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_sub=''; while (( $# )); do case $1 in -4|-6|-br|--brief|-details|-s|-human|-iec|-o|-oneline|-c|--color|-color) shift;; -h|--help) __kimix_sub=help; shift;; -*) printf '%s\012' \"ip: unsupported option: $1\" >&2; return 1;; *) if [[ -z $__kimix_sub ]]; then __kimix_sub=$1; fi; shift;; esac; done; case $__kimix_sub in help) printf '%s\012' 'Usage: ip [addr|link|route|neigh] (Windows equivalents: ipconfig / Get-NetAdapter / route print / arp -a)'; return 0;; '') printf '%s\012' 'Usage: ip [addr|link|route|neigh] (Windows equivalents: ipconfig / Get-NetAdapter / route print / arp -a)' >&2; return 1;; addr|address|a) ipconfig;; link|l) powershell.exe -NoProfile -NonInteractive -Command 'Get-NetAdapter | ForEach-Object { \"$($_.Name) $($_.Status) $($_.LinkSpeed) $($_.MacAddress)\" }';; route|r) route print;; neigh|n) arp -a;; *) printf '%s\012' \"ip: unsupported object: $__kimix_sub (supported: addr link route neigh)\" >&2; return 1;; esac; }; fi\012if declare -F ip >/dev/null; then export -f ip; fi\012ip"},
    {"man", "if ! command -v man >/dev/null 2>&1; then man() { local __kimix_native=''; __kimix_native=$(type -P man) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_rc=0 __kimix_seen=0; while (( $# )); do case $1 in --help) printf '%s\012' 'man: show command help (fallback prints <command> --help)'; return 0;; --) shift; break;; -*) printf '%s\012' \"man: unsupported option for --help fallback: $1\" >&2; return 1;; *) break;; esac; done; while (( $# )); do if [[ $1 =~ ^[0-9]+$ ]]; then shift; continue; fi; __kimix_seen=1; \"$1\" --help || __kimix_rc=1; shift; done; if (( ! __kimix_seen )); then printf '%s\012' 'man: missing command name' >&2; return 1; fi; return $__kimix_rc; }; fi\012if declare -F man >/dev/null; then export -f man; fi\012man"},
    {"systemctl", "if ! command -v systemctl >/dev/null 2>&1; then systemctl() { local __kimix_native=''; __kimix_native=$(type -P systemctl) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_cmd=''; local -a __kimix_names=(); while (( $# )); do case $1 in -q|--quiet|--no-pager|--plain|--full|-l|--no-legend|--no-ask-password|--user|--system|--global) shift;; --type=*) shift;; --) shift; break;; -*) printf '%s\012' \"systemctl: unsupported option: $1\" >&2; return 1;; *) if [[ -z $__kimix_cmd ]]; then __kimix_cmd=$1; else __kimix_names+=(\"$1\"); fi; shift;; esac; done; if [[ -z $__kimix_cmd ]]; then printf '%s\012' 'systemctl: missing subcommand (Windows service equivalents; supported: status start stop restart reload enable disable is-active is-enabled list-units list-unit-files)' >&2; return 1; fi; case $__kimix_cmd in status) (( ${#__kimix_names[@]} )) || { printf '%s\012' 'systemctl: missing unit name' >&2; return 1; }; local __kimix_csv=$(IFS=,; echo \"${__kimix_names[*]}\"); powershell.exe -NoProfile -NonInteractive -Command \"Get-Service -Name '$__kimix_csv' | Format-List Name,DisplayName,Status,StartType\";; start|stop|restart|reload) (( ${#__kimix_names[@]} )) || { printf '%s\012' 'systemctl: missing unit name' >&2; return 1; }; local __kimix_ps='' __kimix_n; for __kimix_n in \"${__kimix_names[@]}\"; do if [[ $__kimix_cmd == stop || $__kimix_cmd == restart || $__kimix_cmd == reload ]]; then __kimix_ps+=\"Stop-Service -Name '$__kimix_n' -ErrorAction Stop; \"; fi; if [[ $__kimix_cmd == start || $__kimix_cmd == restart || $__kimix_cmd == reload ]]; then __kimix_ps+=\"Start-Service -Name '$__kimix_n' -ErrorAction Stop; \"; fi; done; powershell.exe -NoProfile -NonInteractive -Command \"$__kimix_ps\";; enable|disable) (( ${#__kimix_names[@]} )) || { printf '%s\012' 'systemctl: missing unit name' >&2; return 1; }; local __kimix_start=demand; [[ $__kimix_cmd == enable ]] && __kimix_start=auto; local __kimix_rc=0 __kimix_n; for __kimix_n in \"${__kimix_names[@]}\"; do sc.exe config \"$__kimix_n\" start= \"$__kimix_start\" >/dev/null || __kimix_rc=1; done; return $__kimix_rc;; is-active) (( ${#__kimix_names[@]} )) || { printf '%s\012' 'systemctl: missing unit name' >&2; return 1; }; local __kimix_rc=0 __kimix_n; for __kimix_n in \"${__kimix_names[@]}\"; do if sc.exe query \"$__kimix_n\" 2>/dev/null | grep -q 'RUNNING'; then printf '%s\012' active; else printf '%s\012' inactive; __kimix_rc=3; fi; done; return $__kimix_rc;; is-enabled) (( ${#__kimix_names[@]} )) || { printf '%s\012' 'systemctl: missing unit name' >&2; return 1; }; local __kimix_rc=0 __kimix_n; for __kimix_n in \"${__kimix_names[@]}\"; do if sc.exe qc \"$__kimix_n\" 2>/dev/null | grep -q 'DISABLED'; then printf '%s\012' disabled; __kimix_rc=1; else printf '%s\012' enabled; fi; done; return $__kimix_rc;; list-units|list-unit-files) powershell.exe -NoProfile -NonInteractive -Command 'Get-Service | ForEach-Object { \"$($_.Status) $($_.Name) $($_.DisplayName)\" }';; *) printf '%s\012' \"systemctl: unsupported subcommand: $__kimix_cmd (supported: status start stop restart reload enable disable is-active is-enabled list-units list-unit-files)\" >&2; return 1;; esac; }; fi\012if declare -F systemctl >/dev/null; then export -f systemctl; fi\012systemctl"},
    {"sudo", "if ! command -v sudo >/dev/null 2>&1; then sudo() { local __kimix_native=''; __kimix_native=$(type -P sudo) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; if (( $# == 0 )); then printf '%s\012' 'sudo: missing command' >&2; return 1; fi; local __kimix_bash; __kimix_bash=$(type -P bash) || { printf '%s\012' 'sudo: bash not found' >&2; return 1; }; local __kimix_wbash; __kimix_wbash=$(cygpath -w -- \"$__kimix_bash\") || return 1; printf '%s\012' 'sudo: elevating via UAC in a separate window; output is not captured here' >&2; __KIMIX_SUDO_CMD=\"$*\" powershell.exe -NoProfile -NonInteractive -Command \"Start-Process -Verb RunAs -Wait -FilePath '$__kimix_wbash' -ArgumentList '-c', \\$env:__KIMIX_SUDO_CMD\"; local __kimix_rc=$?; return $__kimix_rc; }; fi\012if declare -F sudo >/dev/null; then export -f sudo; fi\012sudo"},
    {"gawk", "if ! command -v gawk >/dev/null 2>&1; then gawk() { local __kimix_native=''; __kimix_native=$(type -P gawk) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; awk \"$@\"; }; fi\012if declare -F gawk >/dev/null; then export -f gawk; fi\012gawk"},
    {"gcat", "if ! command -v gcat >/dev/null 2>&1; then gcat() { local __kimix_native=''; __kimix_native=$(type -P gcat) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; cat \"$@\"; }; fi\012if declare -F gcat >/dev/null; then export -f gcat; fi\012gcat"},
    {"gcomm", "if ! command -v gcomm >/dev/null 2>&1; then gcomm() { local __kimix_native=''; __kimix_native=$(type -P gcomm) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; comm \"$@\"; }; fi\012if declare -F gcomm >/dev/null; then export -f gcomm; fi\012gcomm"},
    {"gcp", "if ! command -v gcp >/dev/null 2>&1; then gcp() { local __kimix_native=''; __kimix_native=$(type -P gcp) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; cp \"$@\"; }; fi\012if declare -F gcp >/dev/null; then export -f gcp; fi\012gcp"},
    {"gcut", "if ! command -v gcut >/dev/null 2>&1; then gcut() { local __kimix_native=''; __kimix_native=$(type -P gcut) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; cut \"$@\"; }; fi\012if declare -F gcut >/dev/null; then export -f gcut; fi\012gcut"},
    {"gdate", "if ! command -v gdate >/dev/null 2>&1; then gdate() { local __kimix_native=''; __kimix_native=$(type -P gdate) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; date \"$@\"; }; fi\012if declare -F gdate >/dev/null; then export -f gdate; fi\012gdate"},
    {"gdf", "if ! command -v gdf >/dev/null 2>&1; then gdf() { local __kimix_native=''; __kimix_native=$(type -P gdf) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; df \"$@\"; }; fi\012if declare -F gdf >/dev/null; then export -f gdf; fi\012gdf"},
    {"gdu", "if ! command -v gdu >/dev/null 2>&1; then gdu() { local __kimix_native=''; __kimix_native=$(type -P gdu) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; du \"$@\"; }; fi\012if declare -F gdu >/dev/null; then export -f gdu; fi\012gdu"},
    {"gegrep", "if ! command -v gegrep >/dev/null 2>&1; then gegrep() { local __kimix_native=''; __kimix_native=$(type -P gegrep) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; egrep \"$@\"; }; fi\012if declare -F gegrep >/dev/null; then export -f gegrep; fi\012gegrep"},
    {"gfgrep", "if ! command -v gfgrep >/dev/null 2>&1; then gfgrep() { local __kimix_native=''; __kimix_native=$(type -P gfgrep) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; fgrep \"$@\"; }; fi\012if declare -F gfgrep >/dev/null; then export -f gfgrep; fi\012gfgrep"},
    {"gfind", "if ! command -v gfind >/dev/null 2>&1; then gfind() { local __kimix_native=''; __kimix_native=$(type -P gfind) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; find \"$@\"; }; fi\012if declare -F gfind >/dev/null; then export -f gfind; fi\012gfind"},
    {"ggrep", "if ! command -v ggrep >/dev/null 2>&1; then ggrep() { local __kimix_native=''; __kimix_native=$(type -P ggrep) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; grep \"$@\"; }; fi\012if declare -F ggrep >/dev/null; then export -f ggrep; fi\012ggrep"},
    {"ghead", "if ! command -v ghead >/dev/null 2>&1; then ghead() { local __kimix_native=''; __kimix_native=$(type -P ghead) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; head \"$@\"; }; fi\012if declare -F ghead >/dev/null; then export -f ghead; fi\012ghead"},
    {"gjoin", "if ! command -v gjoin >/dev/null 2>&1; then gjoin() { local __kimix_native=''; __kimix_native=$(type -P gjoin) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; join \"$@\"; }; fi\012if declare -F gjoin >/dev/null; then export -f gjoin; fi\012gjoin"},
    {"gln", "if ! command -v gln >/dev/null 2>&1; then gln() { local __kimix_native=''; __kimix_native=$(type -P gln) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; ln \"$@\"; }; fi\012if declare -F gln >/dev/null; then export -f gln; fi\012gln"},
    {"gls", "if ! command -v gls >/dev/null 2>&1; then gls() { local __kimix_native=''; __kimix_native=$(type -P gls) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; ls \"$@\"; }; fi\012if declare -F gls >/dev/null; then export -f gls; fi\012gls"},
    {"gmake", "if ! command -v gmake >/dev/null 2>&1; then gmake() { local __kimix_native=''; __kimix_native=$(type -P gmake) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; make \"$@\"; }; fi\012if declare -F gmake >/dev/null; then export -f gmake; fi\012gmake"},
    {"gmkdir", "if ! command -v gmkdir >/dev/null 2>&1; then gmkdir() { local __kimix_native=''; __kimix_native=$(type -P gmkdir) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; mkdir \"$@\"; }; fi\012if declare -F gmkdir >/dev/null; then export -f gmkdir; fi\012gmkdir"},
    {"gmv", "if ! command -v gmv >/dev/null 2>&1; then gmv() { local __kimix_native=''; __kimix_native=$(type -P gmv) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; mv \"$@\"; }; fi\012if declare -F gmv >/dev/null; then export -f gmv; fi\012gmv"},
    {"gpaste", "if ! command -v gpaste >/dev/null 2>&1; then gpaste() { local __kimix_native=''; __kimix_native=$(type -P gpaste) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; paste \"$@\"; }; fi\012if declare -F gpaste >/dev/null; then export -f gpaste; fi\012gpaste"},
    {"greadlink", "if ! command -v greadlink >/dev/null 2>&1; then greadlink() { local __kimix_native=''; __kimix_native=$(type -P greadlink) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; readlink \"$@\"; }; fi\012if declare -F greadlink >/dev/null; then export -f greadlink; fi\012greadlink"},
    {"grealpath", "if ! command -v grealpath >/dev/null 2>&1; then grealpath() { local __kimix_native=''; __kimix_native=$(type -P grealpath) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; realpath \"$@\"; }; fi\012if declare -F grealpath >/dev/null; then export -f grealpath; fi\012grealpath"},
    {"grm", "if ! command -v grm >/dev/null 2>&1; then grm() { local __kimix_native=''; __kimix_native=$(type -P grm) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; rm \"$@\"; }; fi\012if declare -F grm >/dev/null; then export -f grm; fi\012grm"},
    {"grmdir", "if ! command -v grmdir >/dev/null 2>&1; then grmdir() { local __kimix_native=''; __kimix_native=$(type -P grmdir) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; rmdir \"$@\"; }; fi\012if declare -F grmdir >/dev/null; then export -f grmdir; fi\012grmdir"},
    {"gsed", "if ! command -v gsed >/dev/null 2>&1; then gsed() { local __kimix_native=''; __kimix_native=$(type -P gsed) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; sed \"$@\"; }; fi\012if declare -F gsed >/dev/null; then export -f gsed; fi\012gsed"},
    {"gseq", "if ! command -v gseq >/dev/null 2>&1; then gseq() { local __kimix_native=''; __kimix_native=$(type -P gseq) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; seq \"$@\"; }; fi\012if declare -F gseq >/dev/null; then export -f gseq; fi\012gseq"},
    {"gshuf", "if ! command -v gshuf >/dev/null 2>&1; then gshuf() { local __kimix_native=''; __kimix_native=$(type -P gshuf) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; shuf \"$@\"; }; fi\012if declare -F gshuf >/dev/null; then export -f gshuf; fi\012gshuf"},
    {"gsort", "if ! command -v gsort >/dev/null 2>&1; then gsort() { local __kimix_native=''; __kimix_native=$(type -P gsort) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; sort \"$@\"; }; fi\012if declare -F gsort >/dev/null; then export -f gsort; fi\012gsort"},
    {"gsplit", "if ! command -v gsplit >/dev/null 2>&1; then gsplit() { local __kimix_native=''; __kimix_native=$(type -P gsplit) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; split \"$@\"; }; fi\012if declare -F gsplit >/dev/null; then export -f gsplit; fi\012gsplit"},
    {"gstat", "if ! command -v gstat >/dev/null 2>&1; then gstat() { local __kimix_native=''; __kimix_native=$(type -P gstat) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; stat \"$@\"; }; fi\012if declare -F gstat >/dev/null; then export -f gstat; fi\012gstat"},
    {"gtail", "if ! command -v gtail >/dev/null 2>&1; then gtail() { local __kimix_native=''; __kimix_native=$(type -P gtail) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; tail \"$@\"; }; fi\012if declare -F gtail >/dev/null; then export -f gtail; fi\012gtail"},
    {"gtar", "if ! command -v gtar >/dev/null 2>&1; then gtar() { local __kimix_native=''; __kimix_native=$(type -P gtar) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; tar \"$@\"; }; fi\012if declare -F gtar >/dev/null; then export -f gtar; fi\012gtar"},
    {"gtr", "if ! command -v gtr >/dev/null 2>&1; then gtr() { local __kimix_native=''; __kimix_native=$(type -P gtr) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; tr \"$@\"; }; fi\012if declare -F gtr >/dev/null; then export -f gtr; fi\012gtr"},
    {"guniq", "if ! command -v guniq >/dev/null 2>&1; then guniq() { local __kimix_native=''; __kimix_native=$(type -P guniq) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; uniq \"$@\"; }; fi\012if declare -F guniq >/dev/null; then export -f guniq; fi\012guniq"},
    {"gwc", "if ! command -v gwc >/dev/null 2>&1; then gwc() { local __kimix_native=''; __kimix_native=$(type -P gwc) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; wc \"$@\"; }; fi\012if declare -F gwc >/dev/null; then export -f gwc; fi\012gwc"},
    {"gxargs", "if ! command -v gxargs >/dev/null 2>&1; then gxargs() { local __kimix_native=''; __kimix_native=$(type -P gxargs) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; xargs \"$@\"; }; fi\012if declare -F gxargs >/dev/null; then export -f gxargs; fi\012gxargs"},
    {"netcat", "if ! command -v netcat >/dev/null 2>&1; then netcat() { local __kimix_native=''; __kimix_native=$(type -P netcat) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_z=0 __kimix_v=0 __kimix_w='' __kimix_host='' __kimix_port=''; while (( $# )); do case $1 in -z) __kimix_z=1; shift;; -v) __kimix_v=1; shift;; -zv|-vz) __kimix_z=1; __kimix_v=1; shift;; -w) __kimix_w=$2; shift 2;; -w?*) __kimix_w=${1#-w}; shift;; -*) printf '%s\\n' \"nc: unsupported option for /dev/tcp fallback: $1\" >&2; return 1;; *) if [[ -z $__kimix_host ]]; then __kimix_host=$1; elif [[ -z $__kimix_port ]]; then __kimix_port=$1; else printf '%s\\n' 'nc: too many arguments' >&2; return 1; fi; shift;; esac; done; if (( ! __kimix_z )); then printf '%s\\n' 'nc: only -z (zero-I/O scan) mode is supported by this fallback' >&2; return 1; fi; if [[ -z $__kimix_host || -z $__kimix_port ]]; then printf '%s\\n' 'nc: missing host or port' >&2; return 1; fi; if [[ -n $__kimix_w ]]; then timeout \"$__kimix_w\" bash -c 'exec 3<>/dev/tcp/$1/$2' _ \"$__kimix_host\" \"$__kimix_port\" 2>/dev/null; else (exec 3<>/dev/tcp/\"$__kimix_host\"/\"$__kimix_port\") 2>/dev/null; fi; local __kimix_rc=$?; (( __kimix_rc != 0 )) && __kimix_rc=1; if (( __kimix_rc == 0 )); then (( __kimix_v )) && printf '%s\\n' \"Connection to $__kimix_host $__kimix_port port [tcp/*] succeeded!\" >&2; else (( __kimix_v )) && printf '%s\\n' \"nc: connect to $__kimix_host port $__kimix_port (tcp) failed\" >&2; fi; return $__kimix_rc; }; fi\012if declare -F netcat >/dev/null; then export -f netcat; fi\012netcat"},
    {"htop", "if ! command -v htop >/dev/null 2>&1; then htop() { local __kimix_native=''; __kimix_native=$(type -P htop) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_delay=3 __kimix_iters=0 __kimix_batch=0 __kimix_count=0; while (( $# )); do case $1 in -b) __kimix_batch=1; shift;; -d) __kimix_delay=$2; shift 2;; -d?*) __kimix_delay=${1#-d}; shift;; -n) __kimix_iters=$2; shift 2;; -n?*) __kimix_iters=${1#-n}; shift;; -h|--help) printf '%s\012' 'top: display processes (Windows approximation via Get-Process; Ctrl+C quits)'; return 0;; -*) printf '%s\012' \"${FUNCNAME[0]}: unsupported option: $1\" >&2; return 1;; *) shift;; esac; done; __kimix_snapshot() { powershell.exe -NoProfile -NonInteractive -Command 'Get-Process | Sort-Object -Property CPU -Descending | Select-Object -First 25 Id,ProcessName,CPU,WorkingSet | Format-Table -AutoSize'; }; if (( __kimix_batch )); then local __kimix_n=$__kimix_iters; (( __kimix_n > 0 )) || __kimix_n=1; while (( __kimix_count < __kimix_n )); do __kimix_snapshot; __kimix_count=$(( __kimix_count + 1 )); done; return 0; fi; while (( __kimix_iters == 0 || __kimix_count < __kimix_iters )); do clear; __kimix_snapshot; __kimix_count=$(( __kimix_count + 1 )); (( __kimix_iters > 0 && __kimix_count >= __kimix_iters )) && break; sleep \"$__kimix_delay\"; done; }; fi\012if declare -F htop >/dev/null; then export -f htop; fi\012htop"},
    {"rev; rev", "if ! command -v rev >/dev/null 2>&1; then rev() { local __kimix_native=''; __kimix_native=$(type -P rev) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_zero=0; while (( $# )); do case $1 in -0|--zero) __kimix_zero=1; shift;; --) shift; break;; -*) printf '%s\\n' \"rev: unsupported option: $1\" >&2; return 1;; *) break;; esac; done; perl '-Mopen=:std,:encoding(UTF-8)' -e 'my $zero = shift @ARGV; my $failed = 0; sub reverse_fh { my ($fh, $zero) = @_; local $/ = $zero ? qq(\\0) : qq(\\n); while (my $record = <$fh>) { my $ended = $zero ? $record =~ s/\\0\\z// : $record =~ s/\\r?\\n\\z//; print scalar reverse($record); print($zero ? qq(\\0) : qq(\\n)) if $ended } } if (@ARGV) { for my $file (@ARGV) { if (open my $fh, q(<:encoding(UTF-8)), $file) { reverse_fh($fh, $zero); close $fh } else { warn qq(rev: $file: $!\\n); $failed = 1 } } } else { reverse_fh(*STDIN, $zero) } exit $failed' -- \"$__kimix_zero\" \"$@\"; }; fi\012if declare -F rev >/dev/null; then export -f rev; fi\012rev; rev"},
    {"gtimeout 1 true; printf x | rev", "if ! command -v gtimeout >/dev/null 2>&1; then gtimeout() { local __kimix_native=''; __kimix_native=$(type -P gtimeout) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; timeout \"$@\"; }; fi\012if ! command -v rev >/dev/null 2>&1; then rev() { local __kimix_native=''; __kimix_native=$(type -P rev) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_zero=0; while (( $# )); do case $1 in -0|--zero) __kimix_zero=1; shift;; --) shift; break;; -*) printf '%s\\n' \"rev: unsupported option: $1\" >&2; return 1;; *) break;; esac; done; perl '-Mopen=:std,:encoding(UTF-8)' -e 'my $zero = shift @ARGV; my $failed = 0; sub reverse_fh { my ($fh, $zero) = @_; local $/ = $zero ? qq(\\0) : qq(\\n); while (my $record = <$fh>) { my $ended = $zero ? $record =~ s/\\0\\z// : $record =~ s/\\r?\\n\\z//; print scalar reverse($record); print($zero ? qq(\\0) : qq(\\n)) if $ended } } if (@ARGV) { for my $file (@ARGV) { if (open my $fh, q(<:encoding(UTF-8)), $file) { reverse_fh($fh, $zero); close $fh } else { warn qq(rev: $file: $!\\n); $failed = 1 } } } else { reverse_fh(*STDIN, $zero) } exit $failed' -- \"$__kimix_zero\" \"$@\"; }; fi\012if declare -F gtimeout >/dev/null; then export -f gtimeout; fi\012if declare -F rev >/dev/null; then export -f rev; fi\012gtimeout 1 true; printf x | rev"},
    {"rev && tree", "if ! command -v rev >/dev/null 2>&1; then rev() { local __kimix_native=''; __kimix_native=$(type -P rev) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_zero=0; while (( $# )); do case $1 in -0|--zero) __kimix_zero=1; shift;; --) shift; break;; -*) printf '%s\\n' \"rev: unsupported option: $1\" >&2; return 1;; *) break;; esac; done; perl '-Mopen=:std,:encoding(UTF-8)' -e 'my $zero = shift @ARGV; my $failed = 0; sub reverse_fh { my ($fh, $zero) = @_; local $/ = $zero ? qq(\\0) : qq(\\n); while (my $record = <$fh>) { my $ended = $zero ? $record =~ s/\\0\\z// : $record =~ s/\\r?\\n\\z//; print scalar reverse($record); print($zero ? qq(\\0) : qq(\\n)) if $ended } } if (@ARGV) { for my $file (@ARGV) { if (open my $fh, q(<:encoding(UTF-8)), $file) { reverse_fh($fh, $zero); close $fh } else { warn qq(rev: $file: $!\\n); $failed = 1 } } } else { reverse_fh(*STDIN, $zero) } exit $failed' -- \"$__kimix_zero\" \"$@\"; }; fi\012if ! command -v tree >/dev/null 2>&1; then tree() { local __kimix_native=''; __kimix_native=$(type -P tree) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_depth=0 __kimix_all=0 __kimix_dirs=0 __kimix_noreport=0 __kimix_dir=''; while (( $# )); do case $1 in -L) __kimix_depth=$2; shift 2;; -L?*) __kimix_depth=${1#-L}; shift;; -a) __kimix_all=1; shift;; -d) __kimix_dirs=1; shift;; --noreport) __kimix_noreport=1; shift;; --) shift; break;; -*) printf '%s\\n' \"tree: unsupported option for perl fallback: $1\" >&2; return 1;; *) __kimix_dir=$1; shift;; esac; done; [[ -n $__kimix_dir ]] || __kimix_dir=.; perl -e 'my ($maxdepth,$showall,$dirsonly,$noreport,$top)=@ARGV; print qq($top\\n); my ($ndirs,$nfiles)=(0,0); sub walk { my ($path,$prefix,$depth)=@_; return if $maxdepth && $depth>$maxdepth; opendir(my $dh,$path) or return; my @e = grep { ! /^[.][.]?$/ } readdir($dh); closedir($dh); @e = grep { $showall || ! /^[.]/ } @e; @e = grep { ! $dirsonly || -d qq($path/$_) } @e; @e = sort { lc($a) cmp lc($b) } @e; my $n=@e; my $i=0; for my $e (@e) { $i++; my $last = $i==$n; my $full = qq($path/$e); my $isdir = -d $full; if ($isdir) { $ndirs++ } else { $nfiles++ } print $prefix, ($last ? qq(`-- ) : qq(|-- )), $e, qq(\\n); walk($full, $prefix . ($last ? qq(    ) : qq(|   )), $depth+1) if $isdir && ! -l $full } } walk($top,q(),1); my $dw = $ndirs==1 ? q(directory) : q(directories); my $fw = $nfiles==1 ? q(file) : q(files); print qq(\\n$ndirs $dw, $nfiles $fw\\n) unless $noreport' -- \"$__kimix_depth\" \"$__kimix_all\" \"$__kimix_dirs\" \"$__kimix_noreport\" \"$__kimix_dir\"; }; fi\012if declare -F rev >/dev/null; then export -f rev; fi\012if declare -F tree >/dev/null; then export -f tree; fi\012rev && tree"},
    {"timeout 5 rev <<< abc", "if ! command -v rev >/dev/null 2>&1; then rev() { local __kimix_native=''; __kimix_native=$(type -P rev) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_zero=0; while (( $# )); do case $1 in -0|--zero) __kimix_zero=1; shift;; --) shift; break;; -*) printf '%s\\n' \"rev: unsupported option: $1\" >&2; return 1;; *) break;; esac; done; perl '-Mopen=:std,:encoding(UTF-8)' -e 'my $zero = shift @ARGV; my $failed = 0; sub reverse_fh { my ($fh, $zero) = @_; local $/ = $zero ? qq(\\0) : qq(\\n); while (my $record = <$fh>) { my $ended = $zero ? $record =~ s/\\0\\z// : $record =~ s/\\r?\\n\\z//; print scalar reverse($record); print($zero ? qq(\\0) : qq(\\n)) if $ended } } if (@ARGV) { for my $file (@ARGV) { if (open my $fh, q(<:encoding(UTF-8)), $file) { reverse_fh($fh, $zero); close $fh } else { warn qq(rev: $file: $!\\n); $failed = 1 } } } else { reverse_fh(*STDIN, $zero) } exit $failed' -- \"$__kimix_zero\" \"$@\"; }; fi\012if declare -F rev >/dev/null; then export -f rev; fi\012timeout 5 /usr/bin/bash -c 'if ! command -v rev >/dev/null 2>&1; then rev() { local __kimix_native='\"'\"''\"'\"'; __kimix_native=$(type -P rev) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_zero=0; while (( $# )); do case $1 in -0|--zero) __kimix_zero=1; shift;; --) shift; break;; -*) printf '\"'\"'%s\\n'\"'\"' \"rev: unsupported option: $1\" >&2; return 1;; *) break;; esac; done; perl '\"'\"'-Mopen=:std,:encoding(UTF-8)'\"'\"' -e '\"'\"'my $zero = shift @ARGV; my $failed = 0; sub reverse_fh { my ($fh, $zero) = @_; local $/ = $zero ? qq(\\0) : qq(\\n); while (my $record = <$fh>) { my $ended = $zero ? $record =~ s/\\0\\z// : $record =~ s/\\r?\\n\\z//; print scalar reverse($record); print($zero ? qq(\\0) : qq(\\n)) if $ended } } if (@ARGV) { for my $file (@ARGV) { if (open my $fh, q(<:encoding(UTF-8)), $file) { reverse_fh($fh, $zero); close $fh } else { warn qq(rev: $file: $!\\n); $failed = 1 } } } else { reverse_fh(*STDIN, $zero) } exit $failed'\"'\"' -- \"$__kimix_zero\" \"$@\"; }; fi; rev \"$@\"' -- <<< abc"},
    {"xargs rev", "if ! command -v rev >/dev/null 2>&1; then rev() { local __kimix_native=''; __kimix_native=$(type -P rev) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_zero=0; while (( $# )); do case $1 in -0|--zero) __kimix_zero=1; shift;; --) shift; break;; -*) printf '%s\\n' \"rev: unsupported option: $1\" >&2; return 1;; *) break;; esac; done; perl '-Mopen=:std,:encoding(UTF-8)' -e 'my $zero = shift @ARGV; my $failed = 0; sub reverse_fh { my ($fh, $zero) = @_; local $/ = $zero ? qq(\\0) : qq(\\n); while (my $record = <$fh>) { my $ended = $zero ? $record =~ s/\\0\\z// : $record =~ s/\\r?\\n\\z//; print scalar reverse($record); print($zero ? qq(\\0) : qq(\\n)) if $ended } } if (@ARGV) { for my $file (@ARGV) { if (open my $fh, q(<:encoding(UTF-8)), $file) { reverse_fh($fh, $zero); close $fh } else { warn qq(rev: $file: $!\\n); $failed = 1 } } } else { reverse_fh(*STDIN, $zero) } exit $failed' -- \"$__kimix_zero\" \"$@\"; }; fi\012if declare -F rev >/dev/null; then export -f rev; fi\012xargs /usr/bin/bash -c 'if ! command -v rev >/dev/null 2>&1; then rev() { local __kimix_native='\"'\"''\"'\"'; __kimix_native=$(type -P rev) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_zero=0; while (( $# )); do case $1 in -0|--zero) __kimix_zero=1; shift;; --) shift; break;; -*) printf '\"'\"'%s\\n'\"'\"' \"rev: unsupported option: $1\" >&2; return 1;; *) break;; esac; done; perl '\"'\"'-Mopen=:std,:encoding(UTF-8)'\"'\"' -e '\"'\"'my $zero = shift @ARGV; my $failed = 0; sub reverse_fh { my ($fh, $zero) = @_; local $/ = $zero ? qq(\\0) : qq(\\n); while (my $record = <$fh>) { my $ended = $zero ? $record =~ s/\\0\\z// : $record =~ s/\\r?\\n\\z//; print scalar reverse($record); print($zero ? qq(\\0) : qq(\\n)) if $ended } } if (@ARGV) { for my $file (@ARGV) { if (open my $fh, q(<:encoding(UTF-8)), $file) { reverse_fh($fh, $zero); close $fh } else { warn qq(rev: $file: $!\\n); $failed = 1 } } } else { reverse_fh(*STDIN, $zero) } exit $failed'\"'\"' -- \"$__kimix_zero\" \"$@\"; }; fi; rev \"$@\"' --"},
    {"env bash rev <<< abc", "if ! command -v rev >/dev/null 2>&1; then rev() { local __kimix_native=''; __kimix_native=$(type -P rev) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_zero=0; while (( $# )); do case $1 in -0|--zero) __kimix_zero=1; shift;; --) shift; break;; -*) printf '%s\\n' \"rev: unsupported option: $1\" >&2; return 1;; *) break;; esac; done; perl '-Mopen=:std,:encoding(UTF-8)' -e 'my $zero = shift @ARGV; my $failed = 0; sub reverse_fh { my ($fh, $zero) = @_; local $/ = $zero ? qq(\\0) : qq(\\n); while (my $record = <$fh>) { my $ended = $zero ? $record =~ s/\\0\\z// : $record =~ s/\\r?\\n\\z//; print scalar reverse($record); print($zero ? qq(\\0) : qq(\\n)) if $ended } } if (@ARGV) { for my $file (@ARGV) { if (open my $fh, q(<:encoding(UTF-8)), $file) { reverse_fh($fh, $zero); close $fh } else { warn qq(rev: $file: $!\\n); $failed = 1 } } } else { reverse_fh(*STDIN, $zero) } exit $failed' -- \"$__kimix_zero\" \"$@\"; }; fi\012if declare -F rev >/dev/null; then export -f rev; fi\012env /usr/bin/bash -c 'if ! command -v rev >/dev/null 2>&1; then rev() { local __kimix_native='\"'\"''\"'\"'; __kimix_native=$(type -P rev) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_zero=0; while (( $# )); do case $1 in -0|--zero) __kimix_zero=1; shift;; --) shift; break;; -*) printf '\"'\"'%s\\n'\"'\"' \"rev: unsupported option: $1\" >&2; return 1;; *) break;; esac; done; perl '\"'\"'-Mopen=:std,:encoding(UTF-8)'\"'\"' -e '\"'\"'my $zero = shift @ARGV; my $failed = 0; sub reverse_fh { my ($fh, $zero) = @_; local $/ = $zero ? qq(\\0) : qq(\\n); while (my $record = <$fh>) { my $ended = $zero ? $record =~ s/\\0\\z// : $record =~ s/\\r?\\n\\z//; print scalar reverse($record); print($zero ? qq(\\0) : qq(\\n)) if $ended } } if (@ARGV) { for my $file (@ARGV) { if (open my $fh, q(<:encoding(UTF-8)), $file) { reverse_fh($fh, $zero); close $fh } else { warn qq(rev: $file: $!\\n); $failed = 1 } } } else { reverse_fh(*STDIN, $zero) } exit $failed'\"'\"' -- \"$__kimix_zero\" \"$@\"; }; fi; rev \"$@\"' -- <<< abc"},
    {"bash -c 'rev && tree'", "if ! command -v rev >/dev/null 2>&1; then rev() { local __kimix_native=''; __kimix_native=$(type -P rev) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_zero=0; while (( $# )); do case $1 in -0|--zero) __kimix_zero=1; shift;; --) shift; break;; -*) printf '%s\\n' \"rev: unsupported option: $1\" >&2; return 1;; *) break;; esac; done; perl '-Mopen=:std,:encoding(UTF-8)' -e 'my $zero = shift @ARGV; my $failed = 0; sub reverse_fh { my ($fh, $zero) = @_; local $/ = $zero ? qq(\\0) : qq(\\n); while (my $record = <$fh>) { my $ended = $zero ? $record =~ s/\\0\\z// : $record =~ s/\\r?\\n\\z//; print scalar reverse($record); print($zero ? qq(\\0) : qq(\\n)) if $ended } } if (@ARGV) { for my $file (@ARGV) { if (open my $fh, q(<:encoding(UTF-8)), $file) { reverse_fh($fh, $zero); close $fh } else { warn qq(rev: $file: $!\\n); $failed = 1 } } } else { reverse_fh(*STDIN, $zero) } exit $failed' -- \"$__kimix_zero\" \"$@\"; }; fi\012if ! command -v tree >/dev/null 2>&1; then tree() { local __kimix_native=''; __kimix_native=$(type -P tree) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_depth=0 __kimix_all=0 __kimix_dirs=0 __kimix_noreport=0 __kimix_dir=''; while (( $# )); do case $1 in -L) __kimix_depth=$2; shift 2;; -L?*) __kimix_depth=${1#-L}; shift;; -a) __kimix_all=1; shift;; -d) __kimix_dirs=1; shift;; --noreport) __kimix_noreport=1; shift;; --) shift; break;; -*) printf '%s\\n' \"tree: unsupported option for perl fallback: $1\" >&2; return 1;; *) __kimix_dir=$1; shift;; esac; done; [[ -n $__kimix_dir ]] || __kimix_dir=.; perl -e 'my ($maxdepth,$showall,$dirsonly,$noreport,$top)=@ARGV; print qq($top\\n); my ($ndirs,$nfiles)=(0,0); sub walk { my ($path,$prefix,$depth)=@_; return if $maxdepth && $depth>$maxdepth; opendir(my $dh,$path) or return; my @e = grep { ! /^[.][.]?$/ } readdir($dh); closedir($dh); @e = grep { $showall || ! /^[.]/ } @e; @e = grep { ! $dirsonly || -d qq($path/$_) } @e; @e = sort { lc($a) cmp lc($b) } @e; my $n=@e; my $i=0; for my $e (@e) { $i++; my $last = $i==$n; my $full = qq($path/$e); my $isdir = -d $full; if ($isdir) { $ndirs++ } else { $nfiles++ } print $prefix, ($last ? qq(`-- ) : qq(|-- )), $e, qq(\\n); walk($full, $prefix . ($last ? qq(    ) : qq(|   )), $depth+1) if $isdir && ! -l $full } } walk($top,q(),1); my $dw = $ndirs==1 ? q(directory) : q(directories); my $fw = $nfiles==1 ? q(file) : q(files); print qq(\\n$ndirs $dw, $nfiles $fw\\n) unless $noreport' -- \"$__kimix_depth\" \"$__kimix_all\" \"$__kimix_dirs\" \"$__kimix_noreport\" \"$__kimix_dir\"; }; fi\012if declare -F rev >/dev/null; then export -f rev; fi\012if declare -F tree >/dev/null; then export -f tree; fi\012rev && tree"},
    {"watch 'rev'", "if ! command -v watch >/dev/null 2>&1; then watch() { local __kimix_native=''; __kimix_native=$(type -P watch) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_interval=2; while (( $# )); do case $1 in -n) __kimix_interval=$2; shift 2;; -n?*) __kimix_interval=${1#-n}; shift;; -t|-d|--no-title|--color) shift;; --) shift; break;; -*) printf '%s\\n' \"watch: unsupported option: $1\" >&2; return 1;; *) break;; esac; done; if [[ $# -eq 0 ]]; then printf '%s\\n' 'watch: missing command' >&2; return 1; fi; while true; do clear; eval \"$*\"; sleep \"$__kimix_interval\"; done; }; fi\012if ! command -v rev >/dev/null 2>&1; then rev() { local __kimix_native=''; __kimix_native=$(type -P rev) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_zero=0; while (( $# )); do case $1 in -0|--zero) __kimix_zero=1; shift;; --) shift; break;; -*) printf '%s\\n' \"rev: unsupported option: $1\" >&2; return 1;; *) break;; esac; done; perl '-Mopen=:std,:encoding(UTF-8)' -e 'my $zero = shift @ARGV; my $failed = 0; sub reverse_fh { my ($fh, $zero) = @_; local $/ = $zero ? qq(\\0) : qq(\\n); while (my $record = <$fh>) { my $ended = $zero ? $record =~ s/\\0\\z// : $record =~ s/\\r?\\n\\z//; print scalar reverse($record); print($zero ? qq(\\0) : qq(\\n)) if $ended } } if (@ARGV) { for my $file (@ARGV) { if (open my $fh, q(<:encoding(UTF-8)), $file) { reverse_fh($fh, $zero); close $fh } else { warn qq(rev: $file: $!\\n); $failed = 1 } } } else { reverse_fh(*STDIN, $zero) } exit $failed' -- \"$__kimix_zero\" \"$@\"; }; fi\012if declare -F watch >/dev/null; then export -f watch; fi\012if declare -F rev >/dev/null; then export -f rev; fi\012watch 'rev'"},
    {"cd /c/dev/x && rev", "if ! command -v rev >/dev/null 2>&1; then rev() { local __kimix_native=''; __kimix_native=$(type -P rev) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_zero=0; while (( $# )); do case $1 in -0|--zero) __kimix_zero=1; shift;; --) shift; break;; -*) printf '%s\\n' \"rev: unsupported option: $1\" >&2; return 1;; *) break;; esac; done; perl '-Mopen=:std,:encoding(UTF-8)' -e 'my $zero = shift @ARGV; my $failed = 0; sub reverse_fh { my ($fh, $zero) = @_; local $/ = $zero ? qq(\\0) : qq(\\n); while (my $record = <$fh>) { my $ended = $zero ? $record =~ s/\\0\\z// : $record =~ s/\\r?\\n\\z//; print scalar reverse($record); print($zero ? qq(\\0) : qq(\\n)) if $ended } } if (@ARGV) { for my $file (@ARGV) { if (open my $fh, q(<:encoding(UTF-8)), $file) { reverse_fh($fh, $zero); close $fh } else { warn qq(rev: $file: $!\\n); $failed = 1 } } } else { reverse_fh(*STDIN, $zero) } exit $failed' -- \"$__kimix_zero\" \"$@\"; }; fi\012if declare -F rev >/dev/null; then export -f rev; fi\012cd C:/dev/x && rev"},
    {"journalctl -u svc", "journalctl -u svc"},
    {"rev <<< abc", "if ! command -v rev >/dev/null 2>&1; then rev() { local __kimix_native=''; __kimix_native=$(type -P rev) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_zero=0; while (( $# )); do case $1 in -0|--zero) __kimix_zero=1; shift;; --) shift; break;; -*) printf '%s\\n' \"rev: unsupported option: $1\" >&2; return 1;; *) break;; esac; done; perl '-Mopen=:std,:encoding(UTF-8)' -e 'my $zero = shift @ARGV; my $failed = 0; sub reverse_fh { my ($fh, $zero) = @_; local $/ = $zero ? qq(\\0) : qq(\\n); while (my $record = <$fh>) { my $ended = $zero ? $record =~ s/\\0\\z// : $record =~ s/\\r?\\n\\z//; print scalar reverse($record); print($zero ? qq(\\0) : qq(\\n)) if $ended } } if (@ARGV) { for my $file (@ARGV) { if (open my $fh, q(<:encoding(UTF-8)), $file) { reverse_fh($fh, $zero); close $fh } else { warn qq(rev: $file: $!\\n); $failed = 1 } } } else { reverse_fh(*STDIN, $zero) } exit $failed' -- \"$__kimix_zero\" \"$@\"; }; fi\012if declare -F rev >/dev/null; then export -f rev; fi\012rev <<< abc"},
    {"cat <<EOF\012hi\012EOF\012&& rev", "if ! command -v rev >/dev/null 2>&1; then rev() { local __kimix_native=''; __kimix_native=$(type -P rev) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_zero=0; while (( $# )); do case $1 in -0|--zero) __kimix_zero=1; shift;; --) shift; break;; -*) printf '%s\\n' \"rev: unsupported option: $1\" >&2; return 1;; *) break;; esac; done; perl '-Mopen=:std,:encoding(UTF-8)' -e 'my $zero = shift @ARGV; my $failed = 0; sub reverse_fh { my ($fh, $zero) = @_; local $/ = $zero ? qq(\\0) : qq(\\n); while (my $record = <$fh>) { my $ended = $zero ? $record =~ s/\\0\\z// : $record =~ s/\\r?\\n\\z//; print scalar reverse($record); print($zero ? qq(\\0) : qq(\\n)) if $ended } } if (@ARGV) { for my $file (@ARGV) { if (open my $fh, q(<:encoding(UTF-8)), $file) { reverse_fh($fh, $zero); close $fh } else { warn qq(rev: $file: $!\\n); $failed = 1 } } } else { reverse_fh(*STDIN, $zero) } exit $failed' -- \"$__kimix_zero\" \"$@\"; }; fi\012if declare -F rev >/dev/null; then export -f rev; fi\012cat <<EOF && rev\012hi\012EOF\012"},
};

// bash_compatibility_prelude() on win32.
const char k_bash_fix_prelude[] = "if ! command -v gtimeout >/dev/null 2>&1; then gtimeout() { local __kimix_native=''; __kimix_native=$(type -P gtimeout) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; timeout \"$@\"; }; fi\012if ! command -v rev >/dev/null 2>&1; then rev() { local __kimix_native=''; __kimix_native=$(type -P rev) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_zero=0; while (( $# )); do case $1 in -0|--zero) __kimix_zero=1; shift;; --) shift; break;; -*) printf '%s\\n' \"rev: unsupported option: $1\" >&2; return 1;; *) break;; esac; done; perl '-Mopen=:std,:encoding(UTF-8)' -e 'my $zero = shift @ARGV; my $failed = 0; sub reverse_fh { my ($fh, $zero) = @_; local $/ = $zero ? qq(\\0) : qq(\\n); while (my $record = <$fh>) { my $ended = $zero ? $record =~ s/\\0\\z// : $record =~ s/\\r?\\n\\z//; print scalar reverse($record); print($zero ? qq(\\0) : qq(\\n)) if $ended } } if (@ARGV) { for my $file (@ARGV) { if (open my $fh, q(<:encoding(UTF-8)), $file) { reverse_fh($fh, $zero); close $fh } else { warn qq(rev: $file: $!\\n); $failed = 1 } } } else { reverse_fh(*STDIN, $zero) } exit $failed' -- \"$__kimix_zero\" \"$@\"; }; fi\012if ! command -v xdg-open >/dev/null 2>&1; then xdg-open() { local __kimix_native=''; __kimix_native=$(type -P xdg-open) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; start \"$@\"; }; fi\012if ! command -v open >/dev/null 2>&1; then open() { local __kimix_native=''; __kimix_native=$(type -P open) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; start \"$@\"; }; fi\012if ! command -v pbcopy >/dev/null 2>&1; then pbcopy() { local __kimix_native=''; __kimix_native=$(type -P pbcopy) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; clip.exe \"$@\"; }; fi\012if ! command -v pbpaste >/dev/null 2>&1; then pbpaste() { local __kimix_native=''; __kimix_native=$(type -P pbpaste) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; powershell.exe -NoProfile -NonInteractive -Command '[Console]::OutputEncoding=[System.Text.Encoding]::UTF8;[Console]::Out.Write((Get-Clipboard -Raw))' \"$@\"; }; fi\012if ! command -v wget >/dev/null 2>&1; then wget() { local __kimix_native=''; __kimix_native=$(type -P wget) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_url='' __kimix_out='' __kimix_stdout=0; local -a __kimix_args=(); while (( $# )); do case $1 in -O|--output-document) __kimix_out=$2; shift 2;; -O?*) __kimix_out=${1#-O}; shift;; --output-document=*) __kimix_out=${1#*=}; shift;; -q|--quiet) __kimix_args+=(-s); shift;; -c|--continue) __kimix_args+=(-C -); shift;; --no-check-certificate) __kimix_args+=(-k); shift;; -T|--timeout) __kimix_args+=(--max-time \"$2\"); shift 2;; --timeout=*) __kimix_args+=(--max-time \"${1#*=}\"); shift;; -*) printf '%s\\n' \"wget: unsupported option for curl fallback: $1\" >&2; return 1;; *) __kimix_url=$1; shift;; esac; done; if [[ -z $__kimix_url ]]; then printf '%s\\n' 'wget: missing URL' >&2; return 1; fi; if [[ $__kimix_out == '-' ]]; then __kimix_stdout=1; fi; if [[ -z $__kimix_out && $__kimix_stdout -eq 0 ]]; then __kimix_out=${__kimix_url##*/}; [[ -n $__kimix_out ]] || __kimix_out=index.html; fi; if (( __kimix_stdout )); then curl -fSL \"${__kimix_args[@]}\" -- \"$__kimix_url\"; else curl -fSL \"${__kimix_args[@]}\" -o \"$__kimix_out\" -- \"$__kimix_url\"; fi; }; fi\012if ! command -v xclip >/dev/null 2>&1; then xclip() { local __kimix_native=''; __kimix_native=$(type -P xclip) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_out=0; while (( $# )); do case $1 in -o|-out) __kimix_out=1; shift;; -i|-in) shift;; -selection|-d|-display) shift 2;; -selection*|-display*) shift;; -*) printf '%s\\n' \"xclip: unsupported option for clipboard fallback: $1\" >&2; return 1;; *) shift;; esac; done; if (( __kimix_out )); then powershell.exe -NoProfile -NonInteractive -Command '[Console]::OutputEncoding=[System.Text.Encoding]::UTF8;[Cons"
"ole]::Out.Write((Get-Clipboard -Raw))'; else clip.exe; fi; }; fi\012if ! command -v xsel >/dev/null 2>&1; then xsel() { local __kimix_native=''; __kimix_native=$(type -P xsel) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_out=0; while (( $# )); do case $1 in --output) __kimix_out=1; shift;; --input|--clipboard|--primary|--secondary) shift;; --*) printf '%s\\n' \"xsel: unsupported option for clipboard fallback: $1\" >&2; return 1;; -*) case $1 in *o*) __kimix_out=1;; esac; shift;; *) shift;; esac; done; if (( __kimix_out )); then powershell.exe -NoProfile -NonInteractive -Command '[Console]::OutputEncoding=[System.Text.Encoding]::UTF8;[Console]::Out.Write((Get-Clipboard -Raw))'; else clip.exe; fi; }; fi\012if ! command -v wl-copy >/dev/null 2>&1; then wl-copy() { local __kimix_native=''; __kimix_native=$(type -P wl-copy) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; while (( $# )); do case $1 in -*) printf '%s\\n' \"wl-copy: unsupported option for clipboard fallback: $1\" >&2; return 1;; *) shift;; esac; done; clip.exe; }; fi\012if ! command -v wl-paste >/dev/null 2>&1; then wl-paste() { local __kimix_native=''; __kimix_native=$(type -P wl-paste) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; while (( $# )); do case $1 in -n|--no-newline) shift;; -*) printf '%s\\n' \"wl-paste: unsupported option for clipboard fallback: $1\" >&2; return 1;; *) shift;; esac; done; powershell.exe -NoProfile -NonInteractive -Command '[Console]::OutputEncoding=[System.Text.Encoding]::UTF8;[Console]::Out.Write((Get-Clipboard -Raw))'; }; fi\012if ! command -v zip >/dev/null 2>&1; then zip() { local __kimix_native=''; __kimix_native=$(type -P zip) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_archive='' __kimix_level=Optimal __kimix_p='' __kimix_combo='' __kimix_i=0; local -a __kimix_paths=() __kimix_wpaths=() __kimix_split=(); while (( $# )); do if [[ $1 == -[!-]* && ${#1} -gt 2 ]]; then __kimix_combo=${1#-}; __kimix_split=(); shift; for (( __kimix_i=0; __kimix_i<${#__kimix_combo}; __kimix_i++ )); do __kimix_split+=(-${__kimix_combo:__kimix_i:1}); done; set -- \"${__kimix_split[@]}\" \"$@\"; continue; fi; case $1 in -r|-R|--recurse-paths|-q|--quiet) shift;; -0) __kimix_level=NoCompression; shift;; -1) __kimix_level=Fastest; shift;; -[2-9]) shift;; -*) printf '%s\\n' \"zip: unsupported option for Compress-Archive fallback: $1\" >&2; return 1;; *) if [[ -z $__kimix_archive ]]; then __kimix_archive=$1; else __kimix_paths+=(\"$1\"); fi; shift;; esac; done; if [[ -z $__kimix_archive || ${#__kimix_paths[@]} -eq 0 ]]; then printf '%s\\n' 'zip: missing archive name or input paths' >&2; return 1; fi; for __kimix_p in \"${__kimix_paths[@]}\"; do __kimix_wpaths+=(\"$(cygpath -w -- \"$__kimix_p\")\"); done; __kimix_archive=$(cygpath -w -- \"$__kimix_archive\"); __KIMIX_ZIP_LEVEL=$__kimix_level __KIMIX_ZIP_DEST=$__kimix_archive __KIMIX_ZIP_PATHS=$(printf '%s\\n' \"${__kimix_wpaths[@]}\") powershell.exe -NoProfile -NonInteractive -Command 'Add-Type -AssemblyName System.IO.Compression, System.IO.Compression.FileSystem; $level = [System.IO.Compression.CompressionLevel]$env:__KIMIX_ZIP_LEVEL; $dest = $env:__KIMIX_ZIP_DEST; if (Test-Path -LiteralPath $dest) { Remove-Item -LiteralPath $dest -Force }; $zip = [System.IO.Compression.ZipFile]::Open($dest, [System.IO.Compression.ZipArchiveMode]::Create); foreach ($p in ($env:__KIMIX_ZIP_PATHS -split \"`n\")) { $item = Get-Item -LiteralPath $p; $base = $item.Name; if ($item.PSIsContainer) { $root = $item.FullName; Get-ChildItem -LiteralPath $root -Recurse -Force | ForEach-Object { $rel = $_.FullName.Substring($root.Length).TrimStart(\"\\\") -replace \"\\\\\", \"/\"; if ($_.PSIsContainer) { $zip.CreateEntry($base + \"/\" + $rel + \"/\") | Out-Null } else { [System.IO.Compression.ZipFileExtensions]::CreateEntryFromFile($zip, $_.FullName, $base + \"/\" + $rel, $level) | Out-Null } } } else { [System.IO.Compression.ZipFileExtensions]::CreateEn"
"tryFromFile($zip, $item.FullName, $base, $level) | Out-Null } }; $zip.Dispose(); if (Test-Path -LiteralPath $dest) { exit 0 } else { exit 1 }'; }; fi\012if ! command -v nc >/dev/null 2>&1; then nc() { local __kimix_native=''; __kimix_native=$(type -P nc) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_z=0 __kimix_v=0 __kimix_w='' __kimix_host='' __kimix_port=''; while (( $# )); do case $1 in -z) __kimix_z=1; shift;; -v) __kimix_v=1; shift;; -zv|-vz) __kimix_z=1; __kimix_v=1; shift;; -w) __kimix_w=$2; shift 2;; -w?*) __kimix_w=${1#-w}; shift;; -*) printf '%s\\n' \"nc: unsupported option for /dev/tcp fallback: $1\" >&2; return 1;; *) if [[ -z $__kimix_host ]]; then __kimix_host=$1; elif [[ -z $__kimix_port ]]; then __kimix_port=$1; else printf '%s\\n' 'nc: too many arguments' >&2; return 1; fi; shift;; esac; done; if (( ! __kimix_z )); then printf '%s\\n' 'nc: only -z (zero-I/O scan) mode is supported by this fallback' >&2; return 1; fi; if [[ -z $__kimix_host || -z $__kimix_port ]]; then printf '%s\\n' 'nc: missing host or port' >&2; return 1; fi; if [[ -n $__kimix_w ]]; then timeout \"$__kimix_w\" bash -c 'exec 3<>/dev/tcp/$1/$2' _ \"$__kimix_host\" \"$__kimix_port\" 2>/dev/null; else (exec 3<>/dev/tcp/\"$__kimix_host\"/\"$__kimix_port\") 2>/dev/null; fi; local __kimix_rc=$?; (( __kimix_rc != 0 )) && __kimix_rc=1; if (( __kimix_rc == 0 )); then (( __kimix_v )) && printf '%s\\n' \"Connection to $__kimix_host $__kimix_port port [tcp/*] succeeded!\" >&2; else (( __kimix_v )) && printf '%s\\n' \"nc: connect to $__kimix_host port $__kimix_port (tcp) failed\" >&2; fi; return $__kimix_rc; }; fi\012if ! command -v pgrep >/dev/null 2>&1; then pgrep() { local __kimix_native=''; __kimix_native=$(type -P pgrep) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_list=0 __kimix_full=0 __kimix_pat=''; while (( $# )); do case $1 in -l) __kimix_list=1; shift;; -f) __kimix_full=1; shift;; -lf|-fl) __kimix_list=1; __kimix_full=1; shift;; --) shift; break;; -*) printf '%s\\n' \"pgrep: unsupported option for Get-Process fallback: $1\" >&2; return 1;; *) __kimix_pat=$1; shift;; esac; done; if [[ -z $__kimix_pat ]]; then printf '%s\\n' 'pgrep: missing pattern' >&2; return 1; fi; if (( __kimix_full )); then __KIMIX_PAT=$__kimix_pat __KIMIX_LIST=$__kimix_list powershell.exe -NoProfile -NonInteractive -Command '$m = Get-CimInstance Win32_Process | Where-Object { $_.CommandLine -match $env:__KIMIX_PAT }; if ($m) { $m | ForEach-Object { if ($env:__KIMIX_LIST -eq \"1\") { \"$($_.ProcessId) $($_.Name)\" } else { $_.ProcessId } }; exit 0 } else { exit 1 }'; else __KIMIX_PAT=$__kimix_pat __KIMIX_LIST=$__kimix_list powershell.exe -NoProfile -NonInteractive -Command '$m = Get-Process | Where-Object { $_.Name -match $env:__KIMIX_PAT }; if ($m) { $m | ForEach-Object { if ($env:__KIMIX_LIST -eq \"1\") { \"$($_.Id) $($_.Name)\" } else { $_.Id } }; exit 0 } else { exit 1 }'; fi; }; fi\012if ! command -v pkill >/dev/null 2>&1; then pkill() { local __kimix_native=''; __kimix_native=$(type -P pkill) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_full=0 __kimix_pat=''; while (( $# )); do case $1 in -f) __kimix_full=1; shift;; --) shift; break;; -*) printf '%s\\n' \"pkill: unsupported option for Stop-Process fallback: $1\" >&2; return 1;; *) __kimix_pat=$1; shift;; esac; done; if [[ -z $__kimix_pat ]]; then printf '%s\\n' 'pkill: missing pattern' >&2; return 1; fi; if (( __kimix_full )); then __KIMIX_PAT=$__kimix_pat powershell.exe -NoProfile -NonInteractive -Command '$m = Get-CimInstance Win32_Process | Where-Object { $_.CommandLine -match $env:__KIMIX_PAT }; if ($m) { $m | ForEach-Object { Stop-Process -Id $_.ProcessId -Force }; exit 0 } else { exit 1 }'; else __KIMIX_PAT=$__kimix_pat powershell.exe -NoProfile -NonInteractive -Command '$m = Get-Process | Where-Object { $_.Name -match $env:__KIMIX_PAT }; if ($m) { $m | Stop-Process -Force; exit 0 } else { exit 1 }'; fi; }; fi\012if ! command -v t"
"raceroute >/dev/null 2>&1; then traceroute() { local __kimix_native=''; __kimix_native=$(type -P traceroute) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local -a __kimix_args=(); while (( $# )); do case $1 in -n) __kimix_args+=(-d); shift;; -m) __kimix_args+=(-h \"$2\"); shift 2;; -m?*) __kimix_args+=(-h \"${1#-m}\"); shift;; --max-hop=*) __kimix_args+=(-h \"${1#*=}\"); shift;; -w) __kimix_args+=(-w \"$(( $2 * 1000 ))\"); shift 2;; -w?*) __kimix_args+=(-w \"$(( ${1#-w} * 1000 ))\"); shift;; -*) printf '%s\\n' \"traceroute: unsupported option for tracert fallback: $1\" >&2; return 1;; *) __kimix_args+=(\"$1\"); shift;; esac; done; tracert \"${__kimix_args[@]}\"; }; fi\012if ! command -v tree >/dev/null 2>&1; then tree() { local __kimix_native=''; __kimix_native=$(type -P tree) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_depth=0 __kimix_all=0 __kimix_dirs=0 __kimix_noreport=0 __kimix_dir=''; while (( $# )); do case $1 in -L) __kimix_depth=$2; shift 2;; -L?*) __kimix_depth=${1#-L}; shift;; -a) __kimix_all=1; shift;; -d) __kimix_dirs=1; shift;; --noreport) __kimix_noreport=1; shift;; --) shift; break;; -*) printf '%s\\n' \"tree: unsupported option for perl fallback: $1\" >&2; return 1;; *) __kimix_dir=$1; shift;; esac; done; [[ -n $__kimix_dir ]] || __kimix_dir=.; perl -e 'my ($maxdepth,$showall,$dirsonly,$noreport,$top)=@ARGV; print qq($top\\n); my ($ndirs,$nfiles)=(0,0); sub walk { my ($path,$prefix,$depth)=@_; return if $maxdepth && $depth>$maxdepth; opendir(my $dh,$path) or return; my @e = grep { ! /^[.][.]?$/ } readdir($dh); closedir($dh); @e = grep { $showall || ! /^[.]/ } @e; @e = grep { ! $dirsonly || -d qq($path/$_) } @e; @e = sort { lc($a) cmp lc($b) } @e; my $n=@e; my $i=0; for my $e (@e) { $i++; my $last = $i==$n; my $full = qq($path/$e); my $isdir = -d $full; if ($isdir) { $ndirs++ } else { $nfiles++ } print $prefix, ($last ? qq(`-- ) : qq(|-- )), $e, qq(\\n); walk($full, $prefix . ($last ? qq(    ) : qq(|   )), $depth+1) if $isdir && ! -l $full } } walk($top,q(),1); my $dw = $ndirs==1 ? q(directory) : q(directories); my $fw = $nfiles==1 ? q(file) : q(files); print qq(\\n$ndirs $dw, $nfiles $fw\\n) unless $noreport' -- \"$__kimix_depth\" \"$__kimix_all\" \"$__kimix_dirs\" \"$__kimix_noreport\" \"$__kimix_dir\"; }; fi\012if ! command -v say >/dev/null 2>&1; then say() { local __kimix_native=''; __kimix_native=$(type -P say) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; while (( $# )); do case $1 in -*) printf '%s\\n' \"say: unsupported option for SAPI fallback: $1\" >&2; return 1;; *) shift;; esac; done; __KIMIX_SAY_TEXT=$* powershell.exe -NoProfile -NonInteractive -Command 'Add-Type -AssemblyName System.Speech; (New-Object System.Speech.Synthesis.SpeechSynthesizer).Speak($env:__KIMIX_SAY_TEXT)'; }; fi\012if ! command -v python3 >/dev/null 2>&1 || [[ $(type -P python3) == *WindowsApps* ]]; then python3() { local __kimix_native=''; __kimix_native=$(type -P python3) || :; if [[ -n $__kimix_native && $__kimix_native != *WindowsApps* ]]; then \"$__kimix_native\" \"$@\"; return; fi; python \"$@\"; }; fi\012if ! command -v pip3 >/dev/null 2>&1 || [[ $(type -P pip3) == *WindowsApps* ]]; then pip3() { local __kimix_native=''; __kimix_native=$(type -P pip3) || :; if [[ -n $__kimix_native && $__kimix_native != *WindowsApps* ]]; then \"$__kimix_native\" \"$@\"; return; fi; pip \"$@\"; }; fi\012if ! command -v copy >/dev/null 2>&1; then copy() { local __kimix_native=''; __kimix_native=$(type -P copy) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; if [[ $# -lt 2 ]]; then printf '%s\\n' 'copy: missing source or destination' >&2; return 1; fi; cp -R -- \"$@\"; }; fi\012if ! command -v move >/dev/null 2>&1; then move() { local __kimix_native=''; __kimix_native=$(type -P move) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; if [[ $# -lt 2 ]]; then printf '%s\\n' 'move: missing source or destination' >&2; return 1; fi; mv -- \"$@\"; }; fi\012if ! co"
"mmand -v del >/dev/null 2>&1; then del() { local __kimix_native=''; __kimix_native=$(type -P del) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; rm -- \"$@\"; }; fi\012if ! command -v erase >/dev/null 2>&1; then erase() { local __kimix_native=''; __kimix_native=$(type -P erase) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; rm -- \"$@\"; }; fi\012if ! command -v ren >/dev/null 2>&1; then ren() { local __kimix_native=''; __kimix_native=$(type -P ren) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; if [[ $# -ne 2 ]]; then printf '%s\\n' 'ren: exactly two arguments required' >&2; return 1; fi; mv -- \"$1\" \"$2\"; }; fi\012if ! command -v rename >/dev/null 2>&1; then rename() { local __kimix_native=''; __kimix_native=$(type -P rename) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; if [[ $# -ne 2 ]]; then printf '%s\\n' 'rename: exactly two arguments required' >&2; return 1; fi; mv -- \"$1\" \"$2\"; }; fi\012if ! command -v rd >/dev/null 2>&1; then rd() { local __kimix_native=''; __kimix_native=$(type -P rd) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; rmdir -- \"$@\"; }; fi\012if ! command -v md >/dev/null 2>&1; then md() { local __kimix_native=''; __kimix_native=$(type -P md) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; mkdir -p -- \"$@\"; }; fi\012if ! command -v chdir >/dev/null 2>&1; then chdir() { local __kimix_native=''; __kimix_native=$(type -P chdir) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; cd -- \"$@\"; }; fi\012if ! command -v cls >/dev/null 2>&1; then cls() { local __kimix_native=''; __kimix_native=$(type -P cls) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; clear; }; fi\012if ! command -v xcopy >/dev/null 2>&1; then xcopy() { local __kimix_native=''; __kimix_native=$(type -P xcopy) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; cp -r -- \"$@\"; }; fi\012if ! command -v mklink >/dev/null 2>&1; then mklink() { local __kimix_native=''; __kimix_native=$(type -P mklink) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_hard=0 __kimix_link='' __kimix_target=''; while (( $# )); do case $1 in /D|/d|/J|/j) shift;; /H|/h) __kimix_hard=1; shift;; *) if [[ -z $__kimix_link ]]; then __kimix_link=$1; elif [[ -z $__kimix_target ]]; then __kimix_target=$1; else printf '%s\\n' 'mklink: too many arguments' >&2; return 1; fi; shift;; esac; done; if [[ -z $__kimix_link || -z $__kimix_target ]]; then printf '%s\\n' 'mklink: missing link name or target' >&2; return 1; fi; if (( __kimix_hard )); then ln -f -- \"$__kimix_target\" \"$__kimix_link\"; else ln -s -- \"$__kimix_target\" \"$__kimix_link\"; fi; }; fi\012if ! command -v findstr >/dev/null 2>&1; then findstr() { local __kimix_native=''; __kimix_native=$(type -P findstr) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; grep \"$@\"; }; fi\012if ! command -v fc >/dev/null 2>&1; then fc() { local __kimix_native=''; __kimix_native=$(type -P fc) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; diff \"$@\"; }; fi\012if ! command -v where >/dev/null 2>&1; then where() { local __kimix_native=''; __kimix_native=$(type -P where) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; which \"$@\"; }; fi\012if ! command -v tasklist >/dev/null 2>&1; then tasklist() { local __kimix_native=''; __kimix_native=$(type -P tasklist) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; powershell.exe -NoProfile -NonInteractive -Command 'Get-Process | Select-Object Name, Id, CPU, WorkingSet | Format-Table -AutoSize'; }; fi\012if ! command -v taskkill >/dev/null 2>&1; then taskkill() { local __kimix_native=''; __kimix_native=$(type -P taskkill) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_force=0 __kimix_pid='' __kimix_im=''; while (( $# ))"
"; do case $1 in /F|/f) __kimix_force=1; shift;; /IM|/im) __kimix_im=$2; shift 2;; /PID|/pid) __kimix_pid=$2; shift 2;; /*) printf '%s\\n' \"taskkill: unsupported option: $1\" >&2; return 1;; *) printf '%s\\n' \"taskkill: unsupported argument: $1\" >&2; return 1;; esac; done; if [[ -n $__kimix_pid ]]; then __KIMIX_FORCE=$__kimix_force __KIMIX_PID=$__kimix_pid powershell.exe -NoProfile -NonInteractive -Command '$force = $env:__KIMIX_FORCE -eq '1'; if ($env:__KIMIX_PID) { Stop-Process -Id $env:__KIMIX_PID -Force:$force; exit 0 } $procs = Get-Process | Where-Object { $_.Name -eq $env:__KIMIX_IM }; if ($procs) { $procs | Stop-Process -Force:$force; exit 0 } else { exit 1 }'; elif [[ -n $__kimix_im ]]; then __KIMIX_FORCE=$__kimix_force __KIMIX_IM=$__kimix_im powershell.exe -NoProfile -NonInteractive -Command '$force = $env:__KIMIX_FORCE -eq '1'; if ($env:__KIMIX_PID) { Stop-Process -Id $env:__KIMIX_PID -Force:$force; exit 0 } $procs = Get-Process | Where-Object { $_.Name -eq $env:__KIMIX_IM }; if ($procs) { $procs | Stop-Process -Force:$force; exit 0 } else { exit 1 }'; else printf '%s\\n' 'taskkill: missing /PID or /IM' >&2; return 1; fi; }; fi\012if ! command -v systeminfo >/dev/null 2>&1; then systeminfo() { local __kimix_native=''; __kimix_native=$(type -P systeminfo) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; powershell.exe -NoProfile -NonInteractive -Command 'Get-ComputerInfo | Format-List'; }; fi\012if ! command -v watch >/dev/null 2>&1; then watch() { local __kimix_native=''; __kimix_native=$(type -P watch) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_interval=2; while (( $# )); do case $1 in -n) __kimix_interval=$2; shift 2;; -n?*) __kimix_interval=${1#-n}; shift;; -t|-d|--no-title|--color) shift;; --) shift; break;; -*) printf '%s\\n' \"watch: unsupported option: $1\" >&2; return 1;; *) break;; esac; done; if [[ $# -eq 0 ]]; then printf '%s\\n' 'watch: missing command' >&2; return 1; fi; while true; do clear; eval \"$*\"; sleep \"$__kimix_interval\"; done; }; fi\012if ! command -v killall >/dev/null 2>&1; then killall() { local __kimix_native=''; __kimix_native=$(type -P killall) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; if [[ $# -eq 0 ]]; then printf '%s\\n' 'killall: missing process name' >&2; return 1; fi; __KIMIX_NAME=$1 powershell.exe -NoProfile -NonInteractive -Command '$procs = Get-Process | Where-Object { $_.Name -eq $env:__KIMIX_NAME }; if ($procs) { $procs | Stop-Process -Force; exit 0 } else { exit 1 }'; }; fi\012if ! command -v pidof >/dev/null 2>&1; then pidof() { local __kimix_native=''; __kimix_native=$(type -P pidof) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; if [[ $# -eq 0 ]]; then printf '%s\\n' 'pidof: missing process name' >&2; return 1; fi; __KIMIX_NAME=$1 powershell.exe -NoProfile -NonInteractive -Command '$ids = (Get-Process | Where-Object { $_.Name -eq $env:__KIMIX_NAME }).Id; if ($ids) { $ids -join \" \"; exit 0 } else { exit 1 }'; }; fi\012if ! command -v column >/dev/null 2>&1; then column() { local __kimix_native=''; __kimix_native=$(type -P column) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_sep='DEFAULT'; while (( $# )); do case $1 in -t) shift;; -s) __kimix_sep=$2; shift 2;; -s?*) __kimix_sep=${1#-s}; shift;; -*) printf '%s\012' \"column: unsupported option for perl fallback: $1\" >&2; return 1;; *) break;; esac; done; perl -e 'my $sep = shift @ARGV; $sep = qr/\\s+/ if $sep eq \"DEFAULT\"; my @rows; my @max; while (<>) { chomp; my @c = split $sep; push @rows, \\@c; for my $i (0..$#c) { $max[$i] = length($c[$i]) if !defined $max[$i] || length($c[$i]) > $max[$i]; } } for my $r (@rows) { print join(\"  \", map { sprintf(\"%-*s\", $max[$_]//0, $r->[$_]) } 0..$#$r), \"\\n\"; }' \"$__kimix_sep\" \"$@\"; }; fi\012if ! command -v free >/dev/null 2>&1; then free() { local __kimix_native=''; __kimix_native=$(type -P free) || :; if [[ -n $__kimix_native ]]; then \"$__kimix"
"_native\" \"$@\"; return; fi; local __kimix_unit=K; while (( $# )); do case $1 in -b|--bytes) __kimix_unit=B; shift;; -k|--kibi|--kilo) __kimix_unit=K; shift;; -m|--mebi|--mega) __kimix_unit=M; shift;; -g|--gibi|--giga) __kimix_unit=G; shift;; -h|--human) __kimix_unit=H; shift;; --help) printf '%s\012' 'free: report memory usage (Windows: Win32_OperatingSystem; no swap row)'; return 0;; -*) printf '%s\012' \"free: unsupported option: $1\" >&2; return 1;; *) shift;; esac; done; local -a __kimix_m=(); mapfile -t __kimix_m < <(powershell.exe -NoProfile -NonInteractive -Command '$o=Get-CimInstance Win32_OperatingSystem; Write-Output $o.TotalVisibleMemorySize; Write-Output $o.FreePhysicalMemory' 2>/dev/null | tr -d '\\r'); if [[ ${#__kimix_m[@]} -lt 2 ]]; then printf '%s\012' 'free: failed to query memory information' >&2; return 1; fi; local __kimix_total=${__kimix_m[0]} __kimix_free=${__kimix_m[1]}; local __kimix_used=$(( __kimix_total - __kimix_free )); printf '%s\012' '               total        used        free'; case $__kimix_unit in B) printf 'Mem: %12d %11d %11d\012' $(( __kimix_total * 1024 )) $(( __kimix_used * 1024 )) $(( __kimix_free * 1024 ));; M) printf 'Mem: %12d %11d %11d\012' $(( __kimix_total / 1024 )) $(( __kimix_used / 1024 )) $(( __kimix_free / 1024 ));; G) printf 'Mem: %12d %11d %11d\012' $(( __kimix_total / 1024 / 1024 )) $(( __kimix_used / 1024 / 1024 )) $(( __kimix_free / 1024 / 1024 ));; H) awk -v t=\"$__kimix_total\" -v f=\"$__kimix_free\" 'function h(x,  i){i=1; while(x>=10240&&i<4){x=x/1024;i++} return sprintf(\"%.1f%s\", x, substr(\"KMGT\", i, 1))} BEGIN{printf \"Mem: %11s %10s %10s\\n\", h(t), h(t-f), h(f)}';; *) printf 'Mem: %12d %11d %11d\012' \"$__kimix_total\" \"$__kimix_used\" \"$__kimix_free\";; esac; }; fi\012if ! command -v uptime >/dev/null 2>&1; then uptime() { local __kimix_native=''; __kimix_native=$(type -P uptime) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_since=0; while (( $# )); do case $1 in -s|--since) __kimix_since=1; shift;; --help) printf '%s\012' 'uptime: tell how long the system has been running (Windows approximation; no user count)'; return 0;; -*) printf '%s\012' \"uptime: unsupported option: $1\" >&2; return 1;; *) printf '%s\012' \"uptime: unsupported argument: $1\" >&2; return 1;; esac; done; local -a __kimix_u=(); mapfile -t __kimix_u < <(powershell.exe -NoProfile -NonInteractive -Command '$b=(Get-CimInstance Win32_OperatingSystem).LastBootUpTime; Write-Output $b.ToString(\"yyyy-MM-dd HH:mm:ss\"); $up=New-TimeSpan -Start $b -End (Get-Date); Write-Output $up.Days; Write-Output $up.Hours; Write-Output $up.Minutes' 2>/dev/null | tr -d '\\r'); if [[ ${#__kimix_u[@]} -lt 4 ]]; then printf '%s\012' 'uptime: failed to query boot time' >&2; return 1; fi; if (( __kimix_since )); then printf '%s\012' \"${__kimix_u[0]}\"; return 0; fi; local __kimix_days=${__kimix_u[1]} __kimix_hours=${__kimix_u[2]} __kimix_mins=${__kimix_u[3]}; printf -v __kimix_mins '%02d' \"$__kimix_mins\"; local __kimix_up; if (( __kimix_days > 0 )); then printf -v __kimix_up '%d day(s), %d:%s' \"$__kimix_days\" \"$__kimix_hours\" \"$__kimix_mins\"; else printf -v __kimix_up '%d:%s' \"$__kimix_hours\" \"$__kimix_mins\"; fi; printf '%s up %s, load average: n/a (not reported on Windows)\012' \"$(date '+%H:%M:%S')\" \"$__kimix_up\"; }; fi\012if ! command -v top >/dev/null 2>&1; then top() { local __kimix_native=''; __kimix_native=$(type -P top) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_delay=3 __kimix_iters=0 __kimix_batch=0 __kimix_count=0; while (( $# )); do case $1 in -b) __kimix_batch=1; shift;; -d) __kimix_delay=$2; shift 2;; -d?*) __kimix_delay=${1#-d}; shift;; -n) __kimix_iters=$2; shift 2;; -n?*) __kimix_iters=${1#-n}; shift;; -h|--help) printf '%s\012' 'top: display processes (Windows approximation via Get-Process; Ctrl+C quits)'; return 0;; -*) printf '%s\012' \"${FUNCNAME[0]}: unsupported option: $1\" >&2; return 1;; *) shift;; esac; done; __kimix_snapshot() { powershell.exe -NoProfile -NonInteractive -Command '"
"Get-Process | Sort-Object -Property CPU -Descending | Select-Object -First 25 Id,ProcessName,CPU,WorkingSet | Format-Table -AutoSize'; }; if (( __kimix_batch )); then local __kimix_n=$__kimix_iters; (( __kimix_n > 0 )) || __kimix_n=1; while (( __kimix_count < __kimix_n )); do __kimix_snapshot; __kimix_count=$(( __kimix_count + 1 )); done; return 0; fi; while (( __kimix_iters == 0 || __kimix_count < __kimix_iters )); do clear; __kimix_snapshot; __kimix_count=$(( __kimix_count + 1 )); (( __kimix_iters > 0 && __kimix_count >= __kimix_iters )) && break; sleep \"$__kimix_delay\"; done; }; fi\012if ! command -v ss >/dev/null 2>&1; then ss() { local __kimix_native=''; __kimix_native=$(type -P ss) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_stats=0; local -a __kimix_split=(); local __kimix_combo='' __kimix_i=0; while (( $# )); do if [[ $1 == -[!-]* && ${#1} -gt 2 ]]; then __kimix_combo=${1#-}; __kimix_split=(); shift; for (( __kimix_i=0; __kimix_i<${#__kimix_combo}; __kimix_i++ )); do __kimix_split+=(-${__kimix_combo:__kimix_i:1}); done; set -- \"${__kimix_split[@]}\" \"$@\"; continue; fi; case $1 in -s|--summary) __kimix_stats=1; shift;; -t|-u|-l|-n|-a|-p|-e|-m|-r|-i|-x|-4|-6|-T|--tcp|--udp|--listening|--numeric|--all|--process|--extended|--memory|--resolve|--inet|--inet4|--inet6) shift;; --*) shift;; -*) printf '%s\012' \"ss: unsupported option: $1\" >&2; return 1;; *) shift;; esac; done; if (( __kimix_stats )); then netstat -s; else netstat -ano; fi; }; fi\012if ! command -v ip >/dev/null 2>&1; then ip() { local __kimix_native=''; __kimix_native=$(type -P ip) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_sub=''; while (( $# )); do case $1 in -4|-6|-br|--brief|-details|-s|-human|-iec|-o|-oneline|-c|--color|-color) shift;; -h|--help) __kimix_sub=help; shift;; -*) printf '%s\012' \"ip: unsupported option: $1\" >&2; return 1;; *) if [[ -z $__kimix_sub ]]; then __kimix_sub=$1; fi; shift;; esac; done; case $__kimix_sub in help) printf '%s\012' 'Usage: ip [addr|link|route|neigh] (Windows equivalents: ipconfig / Get-NetAdapter / route print / arp -a)'; return 0;; '') printf '%s\012' 'Usage: ip [addr|link|route|neigh] (Windows equivalents: ipconfig / Get-NetAdapter / route print / arp -a)' >&2; return 1;; addr|address|a) ipconfig;; link|l) powershell.exe -NoProfile -NonInteractive -Command 'Get-NetAdapter | ForEach-Object { \"$($_.Name) $($_.Status) $($_.LinkSpeed) $($_.MacAddress)\" }';; route|r) route print;; neigh|n) arp -a;; *) printf '%s\012' \"ip: unsupported object: $__kimix_sub (supported: addr link route neigh)\" >&2; return 1;; esac; }; fi\012if ! command -v man >/dev/null 2>&1; then man() { local __kimix_native=''; __kimix_native=$(type -P man) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_rc=0 __kimix_seen=0; while (( $# )); do case $1 in --help) printf '%s\012' 'man: show command help (fallback prints <command> --help)'; return 0;; --) shift; break;; -*) printf '%s\012' \"man: unsupported option for --help fallback: $1\" >&2; return 1;; *) break;; esac; done; while (( $# )); do if [[ $1 =~ ^[0-9]+$ ]]; then shift; continue; fi; __kimix_seen=1; \"$1\" --help || __kimix_rc=1; shift; done; if (( ! __kimix_seen )); then printf '%s\012' 'man: missing command name' >&2; return 1; fi; return $__kimix_rc; }; fi\012if ! command -v systemctl >/dev/null 2>&1; then systemctl() { local __kimix_native=''; __kimix_native=$(type -P systemctl) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_cmd=''; local -a __kimix_names=(); while (( $# )); do case $1 in -q|--quiet|--no-pager|--plain|--full|-l|--no-legend|--no-ask-password|--user|--system|--global) shift;; --type=*) shift;; --) shift; break;; -*) printf '%s\012' \"systemctl: unsupported option: $1\" >&2; return 1;; *) if [[ -z $__kimix_cmd ]]; then __kimix_cmd=$1; else __kimix_names+=(\"$1\"); fi; shift;; esac; done; if [[ -z $__kimix_cmd ]]; then printf '%s\012' 'systemctl: missing s"
"ubcommand (Windows service equivalents; supported: status start stop restart reload enable disable is-active is-enabled list-units list-unit-files)' >&2; return 1; fi; case $__kimix_cmd in status) (( ${#__kimix_names[@]} )) || { printf '%s\012' 'systemctl: missing unit name' >&2; return 1; }; local __kimix_csv=$(IFS=,; echo \"${__kimix_names[*]}\"); powershell.exe -NoProfile -NonInteractive -Command \"Get-Service -Name '$__kimix_csv' | Format-List Name,DisplayName,Status,StartType\";; start|stop|restart|reload) (( ${#__kimix_names[@]} )) || { printf '%s\012' 'systemctl: missing unit name' >&2; return 1; }; local __kimix_ps='' __kimix_n; for __kimix_n in \"${__kimix_names[@]}\"; do if [[ $__kimix_cmd == stop || $__kimix_cmd == restart || $__kimix_cmd == reload ]]; then __kimix_ps+=\"Stop-Service -Name '$__kimix_n' -ErrorAction Stop; \"; fi; if [[ $__kimix_cmd == start || $__kimix_cmd == restart || $__kimix_cmd == reload ]]; then __kimix_ps+=\"Start-Service -Name '$__kimix_n' -ErrorAction Stop; \"; fi; done; powershell.exe -NoProfile -NonInteractive -Command \"$__kimix_ps\";; enable|disable) (( ${#__kimix_names[@]} )) || { printf '%s\012' 'systemctl: missing unit name' >&2; return 1; }; local __kimix_start=demand; [[ $__kimix_cmd == enable ]] && __kimix_start=auto; local __kimix_rc=0 __kimix_n; for __kimix_n in \"${__kimix_names[@]}\"; do sc.exe config \"$__kimix_n\" start= \"$__kimix_start\" >/dev/null || __kimix_rc=1; done; return $__kimix_rc;; is-active) (( ${#__kimix_names[@]} )) || { printf '%s\012' 'systemctl: missing unit name' >&2; return 1; }; local __kimix_rc=0 __kimix_n; for __kimix_n in \"${__kimix_names[@]}\"; do if sc.exe query \"$__kimix_n\" 2>/dev/null | grep -q 'RUNNING'; then printf '%s\012' active; else printf '%s\012' inactive; __kimix_rc=3; fi; done; return $__kimix_rc;; is-enabled) (( ${#__kimix_names[@]} )) || { printf '%s\012' 'systemctl: missing unit name' >&2; return 1; }; local __kimix_rc=0 __kimix_n; for __kimix_n in \"${__kimix_names[@]}\"; do if sc.exe qc \"$__kimix_n\" 2>/dev/null | grep -q 'DISABLED'; then printf '%s\012' disabled; __kimix_rc=1; else printf '%s\012' enabled; fi; done; return $__kimix_rc;; list-units|list-unit-files) powershell.exe -NoProfile -NonInteractive -Command 'Get-Service | ForEach-Object { \"$($_.Status) $($_.Name) $($_.DisplayName)\" }';; *) printf '%s\012' \"systemctl: unsupported subcommand: $__kimix_cmd (supported: status start stop restart reload enable disable is-active is-enabled list-units list-unit-files)\" >&2; return 1;; esac; }; fi\012if ! command -v sudo >/dev/null 2>&1; then sudo() { local __kimix_native=''; __kimix_native=$(type -P sudo) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; if (( $# == 0 )); then printf '%s\012' 'sudo: missing command' >&2; return 1; fi; local __kimix_bash; __kimix_bash=$(type -P bash) || { printf '%s\012' 'sudo: bash not found' >&2; return 1; }; local __kimix_wbash; __kimix_wbash=$(cygpath -w -- \"$__kimix_bash\") || return 1; printf '%s\012' 'sudo: elevating via UAC in a separate window; output is not captured here' >&2; __KIMIX_SUDO_CMD=\"$*\" powershell.exe -NoProfile -NonInteractive -Command \"Start-Process -Verb RunAs -Wait -FilePath '$__kimix_wbash' -ArgumentList '-c', \\$env:__KIMIX_SUDO_CMD\"; local __kimix_rc=$?; return $__kimix_rc; }; fi\012if ! command -v gawk >/dev/null 2>&1; then gawk() { local __kimix_native=''; __kimix_native=$(type -P gawk) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; awk \"$@\"; }; fi\012if ! command -v gcat >/dev/null 2>&1; then gcat() { local __kimix_native=''; __kimix_native=$(type -P gcat) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; cat \"$@\"; }; fi\012if ! command -v gcomm >/dev/null 2>&1; then gcomm() { local __kimix_native=''; __kimix_native=$(type -P gcomm) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; comm \"$@\"; }; fi\012if ! command -v gcp >/dev/null 2>&1; then gcp() { local __kimix_native=''; __kimix_native=$(type -P gcp) || :; if [[ -n $__kimix_native ]]; th"
"en \"$__kimix_native\" \"$@\"; return; fi; cp \"$@\"; }; fi\012if ! command -v gcut >/dev/null 2>&1; then gcut() { local __kimix_native=''; __kimix_native=$(type -P gcut) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; cut \"$@\"; }; fi\012if ! command -v gdate >/dev/null 2>&1; then gdate() { local __kimix_native=''; __kimix_native=$(type -P gdate) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; date \"$@\"; }; fi\012if ! command -v gdf >/dev/null 2>&1; then gdf() { local __kimix_native=''; __kimix_native=$(type -P gdf) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; df \"$@\"; }; fi\012if ! command -v gdu >/dev/null 2>&1; then gdu() { local __kimix_native=''; __kimix_native=$(type -P gdu) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; du \"$@\"; }; fi\012if ! command -v gegrep >/dev/null 2>&1; then gegrep() { local __kimix_native=''; __kimix_native=$(type -P gegrep) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; egrep \"$@\"; }; fi\012if ! command -v gfgrep >/dev/null 2>&1; then gfgrep() { local __kimix_native=''; __kimix_native=$(type -P gfgrep) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; fgrep \"$@\"; }; fi\012if ! command -v gfind >/dev/null 2>&1; then gfind() { local __kimix_native=''; __kimix_native=$(type -P gfind) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; find \"$@\"; }; fi\012if ! command -v ggrep >/dev/null 2>&1; then ggrep() { local __kimix_native=''; __kimix_native=$(type -P ggrep) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; grep \"$@\"; }; fi\012if ! command -v ghead >/dev/null 2>&1; then ghead() { local __kimix_native=''; __kimix_native=$(type -P ghead) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; head \"$@\"; }; fi\012if ! command -v gjoin >/dev/null 2>&1; then gjoin() { local __kimix_native=''; __kimix_native=$(type -P gjoin) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; join \"$@\"; }; fi\012if ! command -v gln >/dev/null 2>&1; then gln() { local __kimix_native=''; __kimix_native=$(type -P gln) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; ln \"$@\"; }; fi\012if ! command -v gls >/dev/null 2>&1; then gls() { local __kimix_native=''; __kimix_native=$(type -P gls) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; ls \"$@\"; }; fi\012if ! command -v gmake >/dev/null 2>&1; then gmake() { local __kimix_native=''; __kimix_native=$(type -P gmake) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; make \"$@\"; }; fi\012if ! command -v gmkdir >/dev/null 2>&1; then gmkdir() { local __kimix_native=''; __kimix_native=$(type -P gmkdir) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; mkdir \"$@\"; }; fi\012if ! command -v gmv >/dev/null 2>&1; then gmv() { local __kimix_native=''; __kimix_native=$(type -P gmv) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; mv \"$@\"; }; fi\012if ! command -v gpaste >/dev/null 2>&1; then gpaste() { local __kimix_native=''; __kimix_native=$(type -P gpaste) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; paste \"$@\"; }; fi\012if ! command -v greadlink >/dev/null 2>&1; then greadlink() { local __kimix_native=''; __kimix_native=$(type -P greadlink) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; readlink \"$@\"; }; fi\012if ! command -v grealpath >/dev/null 2>&1; then grealpath() { local __kimix_native=''; __kimix_native=$(type -P grealpath) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; realpath \"$@\"; }; fi\012if ! command -v grm >/dev/null 2>&1; then grm() { local __kimix_native=''; __kimix_native=$(type -P grm) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; rm \"$@\"; }; fi\012if ! command -v grmdir >/dev/null 2>&1; then grmdir() { local __kimix_nati"
"ve=''; __kimix_native=$(type -P grmdir) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; rmdir \"$@\"; }; fi\012if ! command -v gsed >/dev/null 2>&1; then gsed() { local __kimix_native=''; __kimix_native=$(type -P gsed) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; sed \"$@\"; }; fi\012if ! command -v gseq >/dev/null 2>&1; then gseq() { local __kimix_native=''; __kimix_native=$(type -P gseq) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; seq \"$@\"; }; fi\012if ! command -v gshuf >/dev/null 2>&1; then gshuf() { local __kimix_native=''; __kimix_native=$(type -P gshuf) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; shuf \"$@\"; }; fi\012if ! command -v gsort >/dev/null 2>&1; then gsort() { local __kimix_native=''; __kimix_native=$(type -P gsort) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; sort \"$@\"; }; fi\012if ! command -v gsplit >/dev/null 2>&1; then gsplit() { local __kimix_native=''; __kimix_native=$(type -P gsplit) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; split \"$@\"; }; fi\012if ! command -v gstat >/dev/null 2>&1; then gstat() { local __kimix_native=''; __kimix_native=$(type -P gstat) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; stat \"$@\"; }; fi\012if ! command -v gtail >/dev/null 2>&1; then gtail() { local __kimix_native=''; __kimix_native=$(type -P gtail) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; tail \"$@\"; }; fi\012if ! command -v gtar >/dev/null 2>&1; then gtar() { local __kimix_native=''; __kimix_native=$(type -P gtar) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; tar \"$@\"; }; fi\012if ! command -v gtr >/dev/null 2>&1; then gtr() { local __kimix_native=''; __kimix_native=$(type -P gtr) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; tr \"$@\"; }; fi\012if ! command -v guniq >/dev/null 2>&1; then guniq() { local __kimix_native=''; __kimix_native=$(type -P guniq) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; uniq \"$@\"; }; fi\012if ! command -v gwc >/dev/null 2>&1; then gwc() { local __kimix_native=''; __kimix_native=$(type -P gwc) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; wc \"$@\"; }; fi\012if ! command -v gxargs >/dev/null 2>&1; then gxargs() { local __kimix_native=''; __kimix_native=$(type -P gxargs) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; xargs \"$@\"; }; fi\012if ! command -v netcat >/dev/null 2>&1; then netcat() { local __kimix_native=''; __kimix_native=$(type -P netcat) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_z=0 __kimix_v=0 __kimix_w='' __kimix_host='' __kimix_port=''; while (( $# )); do case $1 in -z) __kimix_z=1; shift;; -v) __kimix_v=1; shift;; -zv|-vz) __kimix_z=1; __kimix_v=1; shift;; -w) __kimix_w=$2; shift 2;; -w?*) __kimix_w=${1#-w}; shift;; -*) printf '%s\\n' \"nc: unsupported option for /dev/tcp fallback: $1\" >&2; return 1;; *) if [[ -z $__kimix_host ]]; then __kimix_host=$1; elif [[ -z $__kimix_port ]]; then __kimix_port=$1; else printf '%s\\n' 'nc: too many arguments' >&2; return 1; fi; shift;; esac; done; if (( ! __kimix_z )); then printf '%s\\n' 'nc: only -z (zero-I/O scan) mode is supported by this fallback' >&2; return 1; fi; if [[ -z $__kimix_host || -z $__kimix_port ]]; then printf '%s\\n' 'nc: missing host or port' >&2; return 1; fi; if [[ -n $__kimix_w ]]; then timeout \"$__kimix_w\" bash -c 'exec 3<>/dev/tcp/$1/$2' _ \"$__kimix_host\" \"$__kimix_port\" 2>/dev/null; else (exec 3<>/dev/tcp/\"$__kimix_host\"/\"$__kimix_port\") 2>/dev/null; fi; local __kimix_rc=$?; (( __kimix_rc != 0 )) && __kimix_rc=1; if (( __kimix_rc == 0 )); then (( __kimix_v )) && printf '%s\\n' \"Connection to $__kimix_host $__kimix_port port [tcp/*] succeeded!\" >&2; else (( __kimix_v )) && printf '%s\\n' \"nc: connect to $__kimix_host port $__kimi"
"x_port (tcp) failed\" >&2; fi; return $__kimix_rc; }; fi\012if ! command -v htop >/dev/null 2>&1; then htop() { local __kimix_native=''; __kimix_native=$(type -P htop) || :; if [[ -n $__kimix_native ]]; then \"$__kimix_native\" \"$@\"; return; fi; local __kimix_delay=3 __kimix_iters=0 __kimix_batch=0 __kimix_count=0; while (( $# )); do case $1 in -b) __kimix_batch=1; shift;; -d) __kimix_delay=$2; shift 2;; -d?*) __kimix_delay=${1#-d}; shift;; -n) __kimix_iters=$2; shift 2;; -n?*) __kimix_iters=${1#-n}; shift;; -h|--help) printf '%s\012' 'top: display processes (Windows approximation via Get-Process; Ctrl+C quits)'; return 0;; -*) printf '%s\012' \"${FUNCNAME[0]}: unsupported option: $1\" >&2; return 1;; *) shift;; esac; done; __kimix_snapshot() { powershell.exe -NoProfile -NonInteractive -Command 'Get-Process | Sort-Object -Property CPU -Descending | Select-Object -First 25 Id,ProcessName,CPU,WorkingSet | Format-Table -AutoSize'; }; if (( __kimix_batch )); then local __kimix_n=$__kimix_iters; (( __kimix_n > 0 )) || __kimix_n=1; while (( __kimix_count < __kimix_n )); do __kimix_snapshot; __kimix_count=$(( __kimix_count + 1 )); done; return 0; fi; while (( __kimix_iters == 0 || __kimix_count < __kimix_iters )); do clear; __kimix_snapshot; __kimix_count=$(( __kimix_count + 1 )); (( __kimix_iters > 0 && __kimix_count >= __kimix_iters )) && break; sleep \"$__kimix_delay\"; done; }; fi\012if declare -F gtimeout >/dev/null; then export -f gtimeout; fi\012if declare -F rev >/dev/null; then export -f rev; fi\012if declare -F xdg-open >/dev/null; then export -f xdg-open; fi\012if declare -F open >/dev/null; then export -f open; fi\012if declare -F pbcopy >/dev/null; then export -f pbcopy; fi\012if declare -F pbpaste >/dev/null; then export -f pbpaste; fi\012if declare -F wget >/dev/null; then export -f wget; fi\012if declare -F xclip >/dev/null; then export -f xclip; fi\012if declare -F xsel >/dev/null; then export -f xsel; fi\012if declare -F wl-copy >/dev/null; then export -f wl-copy; fi\012if declare -F wl-paste >/dev/null; then export -f wl-paste; fi\012if declare -F zip >/dev/null; then export -f zip; fi\012if declare -F nc >/dev/null; then export -f nc; fi\012if declare -F pgrep >/dev/null; then export -f pgrep; fi\012if declare -F pkill >/dev/null; then export -f pkill; fi\012if declare -F traceroute >/dev/null; then export -f traceroute; fi\012if declare -F tree >/dev/null; then export -f tree; fi\012if declare -F say >/dev/null; then export -f say; fi\012if declare -F python3 >/dev/null; then export -f python3; fi\012if declare -F pip3 >/dev/null; then export -f pip3; fi\012if declare -F copy >/dev/null; then export -f copy; fi\012if declare -F move >/dev/null; then export -f move; fi\012if declare -F del >/dev/null; then export -f del; fi\012if declare -F erase >/dev/null; then export -f erase; fi\012if declare -F ren >/dev/null; then export -f ren; fi\012if declare -F rename >/dev/null; then export -f rename; fi\012if declare -F rd >/dev/null; then export -f rd; fi\012if declare -F md >/dev/null; then export -f md; fi\012if declare -F chdir >/dev/null; then export -f chdir; fi\012if declare -F cls >/dev/null; then export -f cls; fi\012if declare -F xcopy >/dev/null; then export -f xcopy; fi\012if declare -F mklink >/dev/null; then export -f mklink; fi\012if declare -F findstr >/dev/null; then export -f findstr; fi\012if declare -F fc >/dev/null; then export -f fc; fi\012if declare -F where >/dev/null; then export -f where; fi\012if declare -F tasklist >/dev/null; then export -f tasklist; fi\012if declare -F taskkill >/dev/null; then export -f taskkill; fi\012if declare -F systeminfo >/dev/null; then export -f systeminfo; fi\012if declare -F watch >/dev/null; then export -f watch; fi\012if declare -F killall >/dev/null; then export -f killall; fi\012if declare -F pidof >/dev/null; then export -f pidof; fi\012if declare -F column >/dev/null; then export -f column; fi\012if declare -F free >/dev/null; then export -f free; fi\012if declare -F uptime >/dev/null; then export -f uptime; fi\012if declare -F top >/dev/null; then export -f top; fi\012if declare -F ss >/dev/null; then expo"
"rt -f ss; fi\012if declare -F ip >/dev/null; then export -f ip; fi\012if declare -F man >/dev/null; then export -f man; fi\012if declare -F systemctl >/dev/null; then export -f systemctl; fi\012if declare -F sudo >/dev/null; then export -f sudo; fi\012if declare -F gawk >/dev/null; then export -f gawk; fi\012if declare -F gcat >/dev/null; then export -f gcat; fi\012if declare -F gcomm >/dev/null; then export -f gcomm; fi\012if declare -F gcp >/dev/null; then export -f gcp; fi\012if declare -F gcut >/dev/null; then export -f gcut; fi\012if declare -F gdate >/dev/null; then export -f gdate; fi\012if declare -F gdf >/dev/null; then export -f gdf; fi\012if declare -F gdu >/dev/null; then export -f gdu; fi\012if declare -F gegrep >/dev/null; then export -f gegrep; fi\012if declare -F gfgrep >/dev/null; then export -f gfgrep; fi\012if declare -F gfind >/dev/null; then export -f gfind; fi\012if declare -F ggrep >/dev/null; then export -f ggrep; fi\012if declare -F ghead >/dev/null; then export -f ghead; fi\012if declare -F gjoin >/dev/null; then export -f gjoin; fi\012if declare -F gln >/dev/null; then export -f gln; fi\012if declare -F gls >/dev/null; then export -f gls; fi\012if declare -F gmake >/dev/null; then export -f gmake; fi\012if declare -F gmkdir >/dev/null; then export -f gmkdir; fi\012if declare -F gmv >/dev/null; then export -f gmv; fi\012if declare -F gpaste >/dev/null; then export -f gpaste; fi\012if declare -F greadlink >/dev/null; then export -f greadlink; fi\012if declare -F grealpath >/dev/null; then export -f grealpath; fi\012if declare -F grm >/dev/null; then export -f grm; fi\012if declare -F grmdir >/dev/null; then export -f grmdir; fi\012if declare -F gsed >/dev/null; then export -f gsed; fi\012if declare -F gseq >/dev/null; then export -f gseq; fi\012if declare -F gshuf >/dev/null; then export -f gshuf; fi\012if declare -F gsort >/dev/null; then export -f gsort; fi\012if declare -F gsplit >/dev/null; then export -f gsplit; fi\012if declare -F gstat >/dev/null; then export -f gstat; fi\012if declare -F gtail >/dev/null; then export -f gtail; fi\012if declare -F gtar >/dev/null; then export -f gtar; fi\012if declare -F gtr >/dev/null; then export -f gtr; fi\012if declare -F guniq >/dev/null; then export -f guniq; fi\012if declare -F gwc >/dev/null; then export -f gwc; fi\012if declare -F gxargs >/dev/null; then export -f gxargs; fi\012if declare -F netcat >/dev/null; then export -f netcat; fi\012if declare -F htop >/dev/null; then export -f htop; fi";
