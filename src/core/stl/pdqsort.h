#pragma once

// pdqsort (pattern-defeating quicksort) is the default sorting algorithm in
// MSVC, libstdc++, and libc++ since C++17. Just include <algorithm>.
//
// Use std::sort for the standard pdqsort implementation.

#include <algorithm>
